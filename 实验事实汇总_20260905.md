# 遗忘失败分析 — 事实汇总

**截至 2026-09-05。** 本文件只记录已核实的事实、已做的决定、和已知的未决项。所有数值来自实测或权威配置文件,不含推测。

---

## 0. 一句话现状

段 A 十二步中,**第 1–8 步已完成**,第 9 步(NPO、DPO 各扫 16 层)刚启动。五个方法全部训练完毕,四个研究对象(NPO、DPO、GA、GD)全部因灾难性遗忘判为不可行,唯一可行的是阳性对照 RMU。工具链(restore.py + 逐层扫描 + 配对 bootstrap)已通过全部校验。

---

## 1. 研究问题

不是比较哪个遗忘方法效果好,而是诊断**失败的机制**。两类失败:

| 失败类型 | 表现 |
|---|---|
| **遗忘不足** | 模型嘴上说"不知道",知识仍在参数里,可被诱导恢复 |
| **灾难性遗忘** | 模型确实忘了,但通用能力一并被破坏 |

核心技术贡献是 **Layer Restoration**:把遗忘后模型的某一层换回原始权重,测量知识恢复量。这提供**因果**证据(动手换回去看会发生什么),而非相关性证据("这层参数变化大所以重要")。

**2026-09-05 问题改述**(因四个研究对象全部不可行):第 9 步的问题从"哪些层压制了遗忘"改为"**哪些层承载了这次损伤**"。判据、阈值、可行性判定一字未改,改的只是"不可行的方法要不要进入扫层"这条执行分支。

---

## 2. 环境与配置

### 2.1 平台

| 项 | 值 |
|---|---|
| 平台 | RunPod(2026-09-04 从 AutoDL 迁移) |
| 持久卷 | `/workspace`,85 G 配额,跨停机保留 |
| 容器盘 | `/root`,停机清空,只放 `/root/tmp_restore` 和日志 |
| 当前占用 | 34 G / 85 G |
| 峰值预算 | 60 G(实算 56.5 G) |
| 剩余空间判定 | 必须用 `du -s /workspace`,**不能用 `df`**(MooseFS 共享卷,`df` 报的是整个机房剩余量,永不告警) |
| 挂载检查 | `mountpoint -q /workspace`,未挂载时一切写入容器盘、停机全丢 |

### 2.2 模型与依赖

| 项 | 值 |
|---|---|
| 模型 | Llama-3.2-1B-Instruct,16 层,词表 128k |
| 精度 | `bfloat16` |
| 注意力 | `flash_attention_2`(库配置硬编码,不装 flash-attn 就必须覆写 → 数值偏离) |
| torch | 2.4.1(`requirements.txt` 口径) |
| transformers | 4.51.3 |
| flash-attn | 2.6.3 |
| seed | 0 |

### 2.3 训练超参(不可协商)

| 项 | 值 | 备注 |
|---|---|---|
| 有效 batch | 32 | per_device 8 × accum 4 |
| 学习率 | 1e-5 | 不许调 |
| epoch | 10(名义) | 实际 `trainer_state.json` 停在 **≈9.24** |
| `max_steps` | **120** | 已源码核实(`trainer.py:5303/5306`,floor 而非 ceil) |
| `steps_per_epoch` | 12 | 但 `do_sync_step`(`trainer.py:2517`)让每 epoch 实走 13 个优化器步 |
| `save_steps` | **10** | 迁移后从 12 改为 10。权威值硬编码在 `run_pipeline.py:SAVE_STEPS` |
| checkpoint | **12 个**,落在 10, 20, …, 120 | step 120 与训练结束重合 |
| `eval_strategy` | `no` | |
| `eval_on_start` | `false` | |
| `do_eval` | `false` | **2026-09-04 新增**,见 §3 事故 |

> **报告注意**:不能写"训了 10 个 epoch",实际是 9.24。这不是偏离,官方 repro 走同一段代码。

### 2.4 数据集

| 集合 | 内容 | 用途 |
|---|---|---|
| **forget10** | 200 个虚构作家中的 10%,**400 道题** | 遗忘目标 |
| **retain90** | 剩余 90% | 该保留的知识 |
| real_authors | 真实作家常识 | 通用能力 |
| world_facts | 世界常识 | 通用能力 |

### 2.5 评测配置

| 配置 | 指标 | 用途 |
|---|---|---|
| `cheap_v4` | `forget_Q_A_ROUGE` + `extraction_strength` + **`retain_Q_A_ROUGE`**(2026-09-05 新增) | 扫层、移植、等式测试 |
| `full_v4` | 库默认全部 + `exact_memorization` + `mia_min_k_plus_plus`(`mia_reference` 保持注释) | Gate 1a、A 点归档、Gate 1b |
| `utility_v4` | `model_utility` | 三点效用测量 |

全部 400 条评测,不许抽子集(会破坏与官方日志的可比性)。

---

## 3. 已发生的事故(2026-09-04)

**现象**:管道在 NPO 卡死约 10 小时,GPU 完全空闲。

**根因链**:

1. 训练本身两次都成功(120/120 步,loss 55→4.2)
2. 崩的是 `open-unlearning` 的 `train.py` 在 `save_model()` **之后**无条件跑的一次完整 TOFU 评测
3. 该评测由 `do_eval` 控制,硬编码在 `configs/trainer/finetune.yaml:20` 为 `True`。**与 `eval_strategy=no` 是两个不同开关**——transformers 中 `eval_strategy != "no"` 会置 `do_eval=True`,但设为 `"no"` **不会**关闭 `do_eval`
4. 崩溃点 `evaluate_probability()`(`utils.py:98`),即此前为 bf16 打过 patch 的函数
5. 显存三因素叠加:训练刚结束进程仍持有 ~13 G(23.53 G 卡仅剩 1.2 G);该评测用 `per_device_eval_batch_size=16`(库默认);自打的 bf16 patch 强制 logits 升 fp32,128k 词表使张量翻倍
6. `run_pipeline.py:113` 的 OOM 重试对整段输出做 `"CUDA out of memory" in text` 字符串匹配,**不区分训练期 OOM 与训练后评测 OOM**,于是把训练 batch 从 8 降到 4——降错了对象,第二次在同一行同一子步骤("Calculating loss: 2/13")崩溃,显存数字几乎相同

**为什么 Gate 1a 没暴露**:Gate 1a 是干净进程评未训练模型,显存宽裕。

**修复**:训练命令加 `trainer.args.do_eval=false`。安全性已核实——`gate_1b` / `operating_point_sweep` / `utility_at` 全部走独立 `eval.py` 子进程、写到 `saves/eval/`,与崩溃路径 `saves/unlearn/.../checkpoint-120/evals/` 无关。

**损失**:约 10 小时空转 + 约 11 分钟 GPU(两次完整训练被删)。

---

## 4. Gate 1a — 环境保真度(硬停)

**做法**:不训练,直接评原始模型,与 `setup_data.py --eval_logs` 下载的官方日志比对。

| 项 | 值 |
|---|---|
| 容差(概率类 / truth ratio) | 0.01 |
| 容差(ROUGE 及 argmax 敏感量) | 0.02 |
| 不设判据(记录) | AUC 类与 p 值类:`forget_quality`、`privleak`、`mia_*` |
| 结果 | **通过,27 项全覆盖** |

**产出的关键常量**:

$$\text{baseline model\_utility} = 0.599397$$

规则:必须用自评值,**不用下载日志里的 0.5992**。两者应在容差内(否则 Gate 1a 就该停),但自评优先。

---

## 5. 操作点阈值(预注册)

**参照物**:retain90 模型——从未学过 forget 数据的干净模型。

| 项 | 值 |
|---|---|
| 逐样本键 | `rougeL_recall`(**非** `score`;另有 `rouge1_recall`、`rougeL_f1`) |
| 样本数 | 400 |
| 均值 | 0.379074(与 `agg_value` 逐位相同,已核对) |
| bootstrap | n=10000, seed=0 |
| 95% CI | [0.362519, **0.396407**] |
| **阈值** | **0.396407**(取 CI 上界) |

**为何取上界而非点估计**:"首次降到"可能触发在一次抖动上。取上界往宽松方向留余地,判断经得起波动。RMU 的实际轨迹证实了这个担心——见 §8.2 的非单调点。

**训练开始前已算定,不许再动。**

---

## 6. Gate 1b — 外部校验(软,已三重核实)

这是整条管道**唯一的外部校验**(`--eval_logs` 只提供 full 和 retain 的日志,没有 NPO 的)。

**核实方式**(三处独立来源,全部对上):

1. **参照值**:`docs/repro.md` 中 **Llama-3.2-1B-Instruct 架构表格**的 NPO / forget10 行(第 254–263 行)。与代码里的 `GATE1B_REF` 逐位一致
   > ⚠️ 该文档 Llama-2-7b 和 1B 分开列表,容易扫错表
2. **比较逻辑**:`run_pipeline.py:193-206` 的 `gate_1b()` 源码
3. **实测值**:远程读取 `saves/eval/gate1b_..._NPO_final/TOFU_EVAL.json` 原始文件

| 指标 | 官方 repro.md | 我们的实测 | \|diff\| | 阈值 |
|---|---|---|---|---|
| `model_utility` | 0.46 | 0.4018387373079418 | 0.0582 | 0.15 |
| `forget_truth_ratio` | 0.7 | 0.6465660273781799 | 0.0534 | 0.15 |
| `forget_quality` | 0.02 | — | 不设判据(KS p 值量级不稳) | — |

**结果:通过。**

> **重要旁证**:官方自己复现的 NPO `model_utility = 0.46`,已经低于我们的可行性下限 0.479518(约为 baseline 的 76.8%)。这说明"四个方法全不可行"**不是测量事故**——官方数字本身就在这个量级。

---

## 7. 五个方法的训练与操作点

### 7.1 操作点定义

**A** = 首个 `forget_Q_A_ROUGE ≤ 0.396407` 的已评测 checkpoint(全部 12 个 + 最终权重全评,不做两段式)。

**overshoot** = 阈值 − A 点 ROUGE。只标记不重跑(重跑是另一条轨迹,混用会出问题)。**所有恢复量的图都必须标注 overshoot**——overshoot 大的方法 A 点更深入遗忘,恢复量天然偏大。

### 7.2 可行性判据

$$\text{feasible} \iff A \text{ 存在} \ \land \ \text{model\_utility}(A) \ge 0.599397 \times 0.80 = 0.479518$$

$$\text{feasible\_check} \iff \text{model\_utility}(A) \ge 0.599397 \times 0.90 = 0.539457$$

两档一并报告。0.90 那档**唯一**用途就是敏感性复核。

### 7.3 结果总表

| 方法 | A 点 | overshoot | first_ckpt util | **A 点 util** | final util | feasible (0.80) | check (0.90) |
|---|---|---|---|---|---|---|---|
| NPO | step 30 | 0.109 | 0.596666 | **0.394637** | 0.401839 | 否 | 否 |
| **RMU** | step 60 | **0.0005** | 0.573931 | **0.554231** | 0.572515 | **是** | **是** |
| DPO | step 20 | **0.342** | 0.592080 | **0.329726** | 0.562907 | 否 | 否 |
| GA | step 30 | 0.147 | 0.596509 | **0.312790** | **0.000000** | 否 | 否 |
| GD | step 50 | 0.0085 | 0.598335 | **0.461557** | 0.444712 | 否 | 否 |

### 7.4 逐方法解读

**NPO** — 起点 0.597 几乎满分,A 点掉到 0.395 后再未回升(final 0.402)。反直觉:NPO 在文献里以保住 utility 著称,这正是它相对 GA 的卖点。但官方 repro 的 0.46 印证了这个量级是真的。

**RMU** — 全程 0.574 → 0.554 → 0.573,**几乎没动过**,同时达成遗忘目标。overshoot 0.0005 意味着 A 点 ROUGE(0.395953)距阈值(0.396407)仅差 0.00045。这就是"可行"长什么样。

**DPO** — 唯一呈 V 形:0.592 → **0.330**(A 点,谷底) → 0.563(回升)。
> ⚠️ **A 点恰好取在最低谷。** 规则是"第一个越过遗忘阈值的 ckpt",不看 utility,所以合规——但后续所有 DPO 分析都建立在这个最糟糕的时刻上。**报告必须写明。**
> overshoot 0.342 意味着 A 点 ROUGE ≈ 0.054,在一个 checkpoint 间隔内从阈值以上冲到近乎归零。这个 A 深度过冲,恢复量天然虚高,跨方法可比性要打折。

**GA** — final utility **归零**。模型彻底损毁,不是变差。这是"灾难性遗忘"最极端的形态,也是报告里最强的一个证据。

**GD** — A 点 0.461557,下限 0.479518,**仅差 0.017961**。五个里唯一够得着的。门槛不能改(预注册),但"卡在线上"这件事值得单独写——GD 不是崩得稀烂。

### 7.5 结论

**四个研究对象全部不可行。** 它们都能把 ROUGE 压到阈值以下,但压到位的那一刻 utility 已跌破 80% 下限。**不是"忘得不够",是"一忘就伤及无辜"。**

四种失败形态各不相同:GA 直接死亡 / NPO 掉下去再没起来 / DPO 中途崩后回升 / GD 差一点够着 / RMU 全程稳定。**这本身就是段 A 一个完整的结论。**

---

## 8. RMU 完整轨迹(12 个 checkpoint)

阈值 0.396407。

| step | forget_Q_A_ROUGE | |
|---|---|---|
| 10 | 0.669336 | |
| 20 | 0.456252 | |
| **30** | **0.480219** | ⚠️ **非单调,较 step 20 回升** |
| 40 | 0.444747 | |
| 50 | 0.425445 | |
| **60** | **0.395953** | ← **首个越线点 = A**(距阈值 0.00045) |
| 70 | 0.368801 | |
| 80 | 0.346356 | |
| 90 | 0.334117 | |
| 100 | 0.324395 | |
| 110 | 0.325057 | ⚠️ 轻微回升 |
| 120 (final) | 0.322082 | |

**两处非单调点印证了阈值取 CI 上界的决定。** 曲线确实在抖(step 20→30 回升 0.024)。若阈值定得再高一点,A 点可能被这次抖动骗到 step 20 去,而那是个假的越线点。这条曲线值得作为方法论依据写进报告。

---

## 9. 第 7 步 — restore.py 与两个精确等式测试

**目的**:纯工具校验。用两个**答案已知**的极端情形校准工具,再拿它去测未知的东西。

| 测试 | 操作 | 期望 | 结果 |
|---|---|---|---|
| **restore** | RMU 全部 Δθ≠0 参数换回 target | 逻辑上就是 target,`cheap` 结果必须逐位相同 | **PASS** |
| **transplant** | target 装上 RMU 全部 Δθ≠0 参数 | 逻辑上就是 RMU(A=step60),必须逐位相同 | **PASS** |

### 9.1 Δθ≠0 参数集的真实粒度:66 个张量

**比 §2.13 原文描述的"0–7 全 8 层"更细:**

| 范围 | 子参数数 | 缺什么 |
|---|---|---|
| `embed_tokens` | 1 | — |
| 层 0–2 | 各 9 个(全部) | — |
| 层 3–6 | 各 8 个 | 缺 `input_layernorm` |
| 层 7 | 7 个 | 缺两个 layernorm,只剩 mlp + attention |

**发现**:梯度信号衰减**不是均匀断在"层"这个粒度上,而是断在具体参数上**。越靠近第 7 层边界,layernorm 子参数越可能精确保持 target 原值。

**意义**:"层"作为分析单元本身就有粒度损失。这直接呼应段 E 计划的层内拆分(attention / MLP 分别换回),也与 §10.3 的层 7 现象指向同一件事——**当前探针可能太粗**。

---

## 10. 第 8 步 — Gate 2 阳性对照

### 10.1 为什么用 RMU

RMU 的损失函数把更新锚定在层 7,**8–15 层被硬性约束为零改动**。这不是巧合,是设计约束。因此实验之前就知道正确答案,可以反过来检验探针:

- 8–15 层若测出效果 → **误报**,工具坏了
- 0–7 层若一个都测不出 → **漏报**,工具太钝

**这个结论只有 RMU 能给。** NPO、DPO 是全参数训练,16 层随便改,答案事先未知——那正是要做实验的原因,但也意味着看到"不显著"时无法判断是真无关还是探针失灵。

**清单硬性要求 Gate 2 必须排在扫其他方法之前**:门放在被检的东西后面等于没放。

### 10.2 统计方法(代码实测值,`scripts/lib/gate2.py`)

**逐题差值(配对)**:

$$d_i^{(\ell)} = \text{score}_i(M_\ell) - \text{score}_i(M_{\text{unlearn}})$$

其中 $M_\ell$ = 遗忘后模型只把第 $\ell$ 层换回 target,其余 15 层不动。$\text{score}_i$ = 第 $i$ 题的 `rougeL_recall`。

$$\text{score}_i = \frac{\text{LCS}(y_i, \hat y_i)}{|y_i|}$$

**恢复量**:

$$R_\ell = \frac{1}{N}\sum_{i=1}^{N} d_i^{(\ell)}, \quad N = 400$$

**归一化恢复量**:

$$\tilde R_\ell = \frac{R_\ell}{\text{agg}(M_{\text{target}}) - \text{agg}(M_{\text{unlearn}})}$$

**bootstrap**:

$$\bar d^{*(b)} = \frac{1}{N}\sum_{i=1}^{N} d_{j_i}^{(\ell)}, \quad j_i \sim \text{Uniform}\{1,\dots,N\}, \quad b = 1,\dots,B$$

**判据**:

$$\text{significant}(\ell) \iff Q_{\alpha/2}(\bar d^*) > 0$$

| 参数 | 值 |
|---|---|
| `N_RESAMPLES` | **20000**(权威值;首次误写 10000,已修正重跑) |
| `SEED` | 0 |
| `BONFERRONI_N` | 16 |
| `ALPHA` | 0.05 / 16 = **0.003125** |
| 有效 CI 水平 | **0.996875**(与 `constants.restoration.significance.ci_effective` 一致) |
| 下界分位数 | 0.0015625 |
| 上界分位数 | 0.9984375 |
| 取法 | 两侧百分位法(**非** BCa,**非**正态近似) |
| 两轴共用 | 是,retain 轴未单独放松或收紧 |

> **注 1**:区间按**双侧**构造,判定只用**下界**。效果上等价于单侧检验但门槛严一倍(左侧只花了 $\alpha/2$ 额度)。**保守方向,不改,但报告方法部分应写明。**
>
> **注 2**:`SEED=0` 写死,两轴 16 层共用同一组重抽样索引矩阵。每层结论各自成立且可复现,但**16 层的 CI 之间是相关的**。对定性判据无影响;若报告中要比较层与层之间恢复量的**大小排序**,这一点必须写明。
>
> **注 3**:`n=20000` 与 `n=10000` 的结论几乎一致,说明此前的错误未影响定性结论。

### 10.3 结果

| 轴 | significant 层 |
|---|---|
| **forget**(进判据) | 0, 1, 2, 3, 4, 5 |
| **retain**(新增,不进判据) | 0, 1, 2, 3, 4, 5, **7** |

**四条判据全部通过,未触发分支:**

| # | 判据 | 检验什么 | 结果 |
|---|---|---|---|
| 1 | 8–15 层 Δθ 严格为 0 | 事实核对(逐元素比 `state_dict`) | ✓ |
| 2 | 这些层 restoration 恢复量严格为 0 | 实为**评测确定性检查** | ✓ |
| 3 | significant 层全落在 0–7 内 | 探针**不误报** | ✓ |
| 4 | 0–7 至少 1 层 significant | 探针**不漏报**(实际 6 层) | ✓ |

**顺带的确定性检查**:8–15 层那 8 次 restoration 跑的其实是同一个模型。**8 次结果与 RMU baseline 逐位相同,离散度为 0。** 这是整个实验里最便宜也最关键的自检——若非零,后续所有跨方法比较都被污染。

**数据**:`results/gate2_rmu_layer_sweep.json`,两轴完整逐层数据。

### 10.4 核心发现:层 7

| | 值 |
|---|---|
| $\|\Delta\theta_7\|$ | **0.612**(8 层中**最大**) |
| forget 轴 CI | **跨零**,不显著 |
| retain 轴 CI | **[+0.002, +0.056]**,显著 |
| 层 6 | 两轴**均不显著** |

**翻译**:层 7 被改得最狠,但这些改动**没有用于压制该忘的知识,反而损害了该保留的知识**。它承担的是纯粹的连带伤害。

**三重意义**:

1. **"改动大 ≠ 起作用"的直接证据。** 只看参数变化会得出"层 7 最关键"的错误结论。这正是本课题的立论——因果而非相关。**图④(必要性 × 充分性散点)整张图想说的话,已在有标准答案的方法上先行拿到。**
2. **"压制"与"损伤"可以分离。** 层 0–5 兼任两角,层 7 只担损伤。说明理论上存在"只压制不损伤"的可能,只是现有方法没做到。
3. **这个发现只有加了 retain 轴才看得见。** 原单轴设计会把层 7 归入"不显著"后不再理会。

**但也是警告**:层 7 明明被改动,单层探针却测不出。合理解释是压制**冗余分布**在 0–7 八层上,拆掉单独一层其余七层仍撑得住。**探针在损伤集中处会失灵。**

**边缘性提示**:retain 轴 CI 下界 **+0.002**,离零仅两千分之一,是勉强越线的结果。且模式反常——层 6 两轴皆不显著,层 7 却在 retain 轴单独越线;若是梯度衰减,应期望层 6 介于中间。报告措辞应为"勉强达到显著",不与层 0–5 平起平坐。

---

## 11. 关键概念(供报告与答辩)

### 11.1 ROUGE 三个字段

$$C_1 = |\text{词交集}| \quad\text{(不管顺序)}, \qquad C_L = \text{LCS 长度} \quad\text{(顺序必须一致)}$$

恒有 $C_L \le C_1$。设标准答案长 $m$、模型输出长 $n$:

| 字段 | 重合量 | 除以 | 特点 |
|---|---|---|---|
| `rouge1_recall` | $C_1$ | $m$ | 最宽松 |
| **`rougeL_recall`** ← **本实验使用** | $C_L$ | $m$ | 顺序敏感,不罚啰嗦 |
| `rougeL_f1` | $C_L$ | 两边 | 最严 |

**为何用 L 不用 1**:被搞坏的模型典型症状是**词还在、话散了**(吐相关词碎片但组不成句)。ROUGE-1 会给虚高分,ROUGE-L 能识别。

**为何用 recall 不用 f1**:测的是"知识还在不在",不是"回答质量好不好"。答对但啰嗦时 f1 会扣分 → 把"话多"误判成"忘了" → **系统性高估遗忘效果**,而本课题恰恰要揭露"看起来忘了其实没忘"。

> **可用旁证**:$C_1$ 高而 $C_L$ 低,本身就是"知识未删除、仅输出被打乱"的信号。`rouge1_recall` 已存在 `value_by_index` 里,**不需重跑**。

**已知局限**:ROUGE 只对字面。模型答"她生于法国首都"意思全对但无"巴黎"二字,会得低分。这是 TOFU 的已知局限,官方同样如此。**报告应写明:测的严格说是"字面复现能力",不是"知识是否存在"。** 段 F 的 relearning 曲线正是为补这个洞。

### 11.2 `agg_value` vs `value_by_index`

| 字段 | 内容 |
|---|---|
| `agg_value` | 一个总分。所有下游判断(阈值、A 点、Gate 判定)都用它 |
| `value_by_index` | 逐题明细,每条是字典:`{rouge1_recall, rougeL_f1, rougeL_recall, input, ground_truth, generation}` |

信息量**单向损失**:有明细能推总分,反之不能。

**bootstrap 需要 $N$ 个逐题数才能重抽。只有 `agg_value` 的指标做不了显著性检验。**

已核实:`forget_Q_A_ROUGE` 的 `agg_value` 就是 400 题 `rougeL_recall` 的平均(与 0.379074 逐位相同)。

### 11.3 `model_utility` 为何不可用于逐层分析

**是什么**:9 个子指标的**调和平均**——三个数据集(retain / real_authors / world_facts)× 三个指标(Q_A_Prob / Q_A_ROUGE / Truth_Ratio)。调和平均的特性是任一项崩溃即拉低总分,适合当"通用能力有没有崩"的报警器。

**为何不能配对 bootstrap**(两个独立原因):

1. **只有 `agg_value`,没有 `value_by_index`** → 凑不出逐题差值
2. **9 项跨三个不同数据集** → A 库第 7 题与 B 库第 7 题是两道不相干的题,结构上不存在"同一题"

> ⚠️ **常见错误理由**:"混了 KS 检验 p 值这类非逐样本的量"——**错**。KS 检验对应的是 `forget_quality`,是独立指标,**不在 `model_utility` 内**。9 项每一项都是逐题可算的。**结论对,理由错。**

**成本**:一次约 45 分钟。16 层 × 2 方法 = **24 小时**。

---

## 12. 已做的决定(全部在看到相关结果之前)

| # | 日期 | 决定 | 理由 |
|---|---|---|---|
| 1 | 09-04 | 训练加 `do_eval=false` | 关掉库自带的多余且必崩的训练后评测。已核实不影响任何判据的产物 |
| 2 | 09-04 | `save_steps` 12 → 10 | 迁移到 85 G 卷,分辨率更高、overshoot 更小 |
| 3 | 09-05 | **GA、GD 的 A 点不删** | 泄压阀规则的触发条件是"因而根本不扫层",而它们的扫层只是推迟到段 B。且 §2.15 规定删除后重训不是同一条轨迹,不可逆 |
| 4 | 09-05 | 第 9 步仍扫 NPO、DPO(尽管判不可行) | 四个研究对象全不可行,只剩阳性对照可扫则主分析无对象。**问题改述为"哪些层承载损伤"**,判据阈值一字未改 |
| 5 | 09-05 | GA、GD 扫层留在段 B | 维持清单原结构,不提前拉入段 A |
| 6 | 09-05 | **效用轴用 `retain_Q_A_ROUGE`,不用 `model_utility`** | 前者逐题、可配对、复用现成 bootstrap;后者结构上不可配对且 24 小时成本。**语义也更对题**:损伤的直接受害者就是本该记住却被误伤的知识 |
| 7 | 09-05 | `model_utility` 不进逐层扫描 | 同上 |
| 8 | 09-05 | 联合换回排除 `embed_tokens` 和 `model.norm` | 两边必须覆盖同一批参数才能比较;否则是拿"16 层的和"比"16 层+嵌入+norm 的联合" |
| 9 | 09-05 | **二分为探索性** | 触发条件看数据定,故:不进主判据、不进跨方法比较、**Bonferroni 分母锁 16 不因追加检验而变**。须写入 `constants-v4.yaml` |

---

## 13. 第 9 步 — 当前状态

**任务**:NPO、DPO 各扫 16 层,机制与 Gate 2 完全相同(同机制才可比)。

**两轴同测**:

| 轴 | 指标 | $R_\ell > 0$ 的含义 |
|---|---|---|
| forget | `forget_Q_A_ROUGE` | 该层承载了**压制** |
| retain | `retain_Q_A_ROUGE` | 该层承载了**损伤** |

**顺带记录**:每层 $\|\Delta\theta_\ell\|$(纯数学,不跑评测)。与 $R_\ell$ 同图即图①。

**额外一次"联合换回"**:0–15 全部 16 层换回 target,`embed_tokens` 和 `model.norm` 保持遗忘后权重。

三个量的关系:

| 量 | 怎么来 | 回答什么 |
|---|---|---|
| 16 层单层加总 $\sum_\ell R_\ell$ | 扫完直接加 | — |
| **16 层联合换回** | **需实测** | 与加总比较 → 可加 or 冗余 |
| 全换回 = 100% | 已知(即 target,归一化的分母) | 与联合比较 → 有多少损伤不在 transformer 层内 |

- **加总 ≈ 联合** → 压制**可加定位**,逐层扫描抓到了全貌
- **加总 ≪ 联合** → 压制**冗余分布**,单层探针漏掉了东西 → 启用累积换回 + 二分

> 二分做法:0–15 一起换回 → 有效果 → 劈成 0–7 / 8–15 → 继续劈。

**给 agent 的要求**:先单测一层计时并上报;扫完把 16 个单层恢复量与联合恢复量一起报回,**不要自行判断是否启用二分**。

---

## 14. 未决与风险

### 14.1 文档漂移(待修)

| # | 位置 | 问题 |
|---|---|---|
| 1 | `执行清单_v4.md` §2.14 第①点 | 仍写 `expected_checkpoints=10` **"本来就是对的"**,但同文档第②点与 constants 均已改为 12 |
| 2 | `执行清单_v4.md` §3 Gate 1b | 仍写判据为 `forget_Q_A_ROUGE`;constants 已于 09-03 修正为 `forget_truth_ratio`(repro.md 的 1B 表格**没有** `forget_Q_A_ROUGE` 列,旧判据够不着参照物) |
| 3 | `constants-v4.yaml` | `verified_from_trainer_state` 仍为 `false`。五方法均已训完,该确认早该回填 |
| 4 | 架构 | `save_steps` 权威值硬编码在 `run_pipeline.py:SAVE_STEPS`,**不读 constants**。与"constants 是唯一真相来源"冲突 |
| 5 | 待补 | 决定 #9(二分探索性护栏)须写入 `constants-v4.yaml` |

### 14.2 方法论风险

| 风险 | 说明 |
|---|---|
| **探针在损伤集中处失灵** | 层 7 已证实。NPO、DPO 处于灾难性遗忘状态且**无标准答案**,看到"不显著"无法区分真无关与探针太钝。这是启用联合换回对比的直接理由 |
| **DPO 的 A 点在谷底** | A = step 20 恰为 utility 最低点(0.330),且 overshoot 0.342 意味 A 点 ROUGE ≈ 0.054。所有 DPO 分析都建立在这个最糟时刻上,跨方法可比性须打折 |
| **失效的保护** | 原设计有"若第一个评测点就已越线则回头细化"的处理。`save_steps=10` 下第 10 步前无 ckpt,**无法执行**。85 G 卷下同样无解(`save_steps=4` 需 30 ckpt = 75 G)。写进局限 |
| **层 7 retain CI 边缘** | 下界 +0.002,措辞须为"勉强达到显著" |
| **CI 相关性** | `SEED=0` 全局共享,16 层 CI 相关。比较层间大小排序时必须说明 |
| **单侧/双侧** | 双侧构造 + 只用下界 = 比单侧严一倍。保守方向,报告写明 |

### 14.3 核实深度不足之处

Gate 2 的统计代码**只有一处来源**:agent 自己贴的代码文本。与 Gate 1b 的三重独立核实(原始 json + 源码 + repro.md 原文)不同,当前无法排除"先用错的版本跑完、被问后再改对贴出"的可能。

若需补强:要求给出 `results/gate2_rmu_layer_sweep.json` 中记录的运行参数,或用同 seed 重跑一层验证数字可复现。

### 14.4 Agent 行为模式(值得持续关注)

第 7–8 步内出现**三次同类错误**:凭执行清单散文描述的印象写代码,未查权威配置文件。

| 错误 | 权威值所在 |
|---|---|
| `value_by_index` 格式当作未知去"防御性猜测" | `constants` 早已写明 `value_by_index_key: "rougeL_recall"` 并附"坑"提醒 |
| `bootstrap_n` 写成 10000 | `constants.restoration.significance.bootstrap_n = 20000` |
| `model_utility` 不可配对的**理由**错误 | KS p 值属 `forget_quality`,不在 `model_utility` 内 |

三次中两次由 agent 自查抓回,说明自查有效,但每次多耗一轮。**第 9 步 sweep 规模是第 8 步的两倍,已要求开工前先列常量对账。**

**正面记录**:2026-09-05 agent 正确驳回了一条上游错误指令(要求 16 层全测 `model_utility`),并给出更准确的替代方案(联合换回排除 embedding/norm),同时主动指出该方案会把二分触发条件从预注册变为看数据定并提议护栏。这比自查实现错误的价值更高。

---

## 15. 后续步骤

| 步 | 内容 | 状态 |
|---|---|---|
| 9 | NPO、DPO 各 16 层扫描 + 联合换回 | **进行中** |
| 10 | 移植:各方法恢复量最高层反方向做一次 | 未开始 |
| 11 | 出图 4 张 | 未开始 |
| 12 | 写报告(8 页) | 未开始 |

**四张图**:

1. 逐层恢复量 vs $\|\Delta\theta_\ell\|$(多方法叠加,**须带误差棒**——两点误差棒重叠则不可比大小)
2. 换回哪层能修回效用
3. 各方法在 A 点的 ES / MinK++ 对比
4. 必要性 × 充分性散点

**第 10 步移植的意义**:恢复量测的是**必要性**(拿掉它遗忘就没了),移植测的是**充分性**(只要它就够了)。两者合起来才是完整的因果论证。

**可选增量段**:

| 段 | 内容 | 意义 |
|---|---|---|
| B | GA、GD 扫层 | 跨族趋势(A 点已保留,随时可做) |
| C | RMU_downproj | 更严的阳性对照:13 层已知为零 + 3 个已知非零矩阵,两头都有标准答案 |
| D | Fisher Overlap | "参数重要性能否预测因果位置"——**两种结果都是结论** |
| E | 层内拆分(attention / MLP) | 直接呼应 §9.1 的 66 张量粒度发现 |
| F | relearning 单曲线 | **40 条数据微调后知识多快回来**——"看起来忘了其实没删"最有冲击力的证据。**不要用 GA**(它不回弹很可能是模型已坏,这一轴上是混淆的) |

**所有恢复量的图必须标注 overshoot。** 跨方法比恢复量的前提是各方法 overshoot 相当,而 DPO 的 0.342 与 RMU 的 0.0005 相差三个量级。
