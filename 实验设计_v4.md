# 遗忘失败分析 — 实验设计 v4（给人看的）

**一句话目标**：不是比较哪个遗忘方法效果好，而是搞清楚它们**为什么失败**，并且用因果证据而不只是相关性来支撑。

失败分两种，两种都要能诊断：

- **灾难性遗忘** — 模型被搞坏了，通用能力崩塌
- **遗忘不足** — 模型看起来忘了，但知识还在，稍加微调就回来

---

## 文档分工

| 文件 | 内容 | 读者 |
|---|---|---|
| **本文件** | 设计理由、砍取依据、局限、汇报口径、术语表 | 人（你 + 汇报） |
| `执行清单_v4.md` | 库里的坑、判据、执行顺序、停机条件 | 执行者（人或 agent） |
| `constants-v4.yaml` | 所有阈值和常数 | 两者共同引用 |

**不要在两处维护同一个数值或同一条命令。** 本文件不含任何可执行命令——v3.1 把阶段 0–1 的命令留在人类版，结果那条 Gate 1a 的 `paths.output_dir` 就漂移成了错的（少了 `../`，会把产物写进库目录）。v4 把命令全部交给执行清单。

---

## v4 相对 v3.1：砍了什么，为什么

**触发砍取的是师兄的两句话：**

> "这个有点像我之前的文章了😅 我之前就是用了 fisher 和 relearning"
> "方向是对的，fisher 就是一个类似的工具"

外加他自己给的优先级排序：**Activation Patching 第 1、Layer/Block Restoration 第 2**，Gradient Conflict + Fisher Overlap 掉到第 4。

结论很清楚：**Fisher + relearning 这条线他做过，重做拿不到差异化；而 v3.1 的工程量有一半以上花在这两条线上。投入和差异化是反着的。**

| 砍掉 | 理由 |
|---|---|
| 梯度冲突 | 只对 GD 一个方法有定义；只能诊断灾难性遗忘（一个什么都不做的方法冲突为零、模型完好、知识一点没少，这个指标给它满分）；师兄早就否掉过它作主方向 |
| Fisher 的 $D$ / $C$ / 随机方向对照 / 验证一 | 师兄两篇论文的核心工具。Fisher Overlap 保留（见段 D），因为它便宜且能和 Restoration 形成"相关 vs 因果"的对照 |
| relearning 全套 + 验证二 | 同上。降级为单方法的一条曲线（段 F） |
| SimNPO | 无 repro 参考值，且是第三个 preference 方法，故事价值最低 |
| Activation Patching | 跨模型搬激活在跑崩的模型上是分布外的，两周内做不了。写进报告作 next step |
| 3B | 24G 卡跑不了全参数微调，需要另租 48G 卡 |
| **操作点 B / B_check** | v3.1 里 B 是「效用侧操作点」，与 A 配对做两套对齐。但 v4 把 $D$/$C$ 和验证一砍掉之后，**没有任何下游分析消费 B**（全部锚在 A 上，`feasible` 只用 utility(A)）。而要正确定出 B，必须在每个 ckpt 上都测 `model_utility`（600 条生成，每方法约 45 分钟）；若只在 A 附近的点上测再挑「最后一个达标的」，B 会被系统性地拉到 A 的位置，那是个错的 B，比没有更糟。改为：utility 只在**第一个 ckpt / A / 最终权重**三点上测，用途是判可行性、画权衡曲线的三点骨架、以及支撑 GA「utility 在越线之前就崩了」那条证据 |

**改掉的两处（不只是删减）：**

1. **Gate 2 第 4 条从"停机"改成"分支"。** 原判据是"0–7 层里至少 1 层显著，否则 pipeline 什么都没检测到"。但两个精确等式测试已经从机械上证明了 restoration 管道可用，所以此时第 4 条失败**不可能**是管道故障，只可能是**单层探针不够灵敏**——RMU 改了 0–7 全部 8 层加 embedding，压制很可能是冗余分布的，换回任何单层都不显著是完全合理的物理结果。原写法会把"知识是分布式的"这个结论直接判成 bug 并停机，而阶段 3 是全部差异化所在。改法：前三条（特异性）任一失败才停机；第四条失败改走**累积换回**（0–7 整体 → 二分），多 4–6 次评测。

2. **执行顺序改成"前缀式"。** 段 A 跑完就是一个完整可交付的 pilot，之后每段都是独立可加、独立可讲的增量。不存在"做了一半白做"的段落，也不必现在就决定投入多少时间。

---

## 为什么主证据是 Layer Restoration

**工程上就是 `state_dict` 替换 + 重跑 eval。** Llama-3.2-1B 有 16 层。

- **必要性**（restoration）：把遗忘后模型的第 k 层参数换回 target 的值，其余不动，看知识回不回来
- **充分性**（transplant）：反方向，把遗忘后模型的第 k 层移植进 target，看遗忘效果转不转移

两者都成立的层，才是干净的压制点。

**它比测量类工具强在哪**：Fisher、CKA、$\Delta W$ 范数这些都只能说"这层相关"；干预能说"就是这层"。这也是师兄把它排在第 2 位的原因。

**成本比想象的低**：16 层全扫每方法只有 16 次**评测**（不训练），1B 上一次 3–4 分钟，全扫约 1 小时。所以不做收窄——为了省几次评测去写二分搜索逻辑，人工时间远超省下的 GPU 时间。

---

## 三个不能省的东西

**① 阳性对照（RMU）**

RMU 的两项 loss 都建在 `model.layers.7` 的输出激活上，**loss 的计算图止于第 7 层**。所以 8–15 层的 `.grad` 是 `None`，AdamW 整个跳过它们，`weight_decay` 也不作用——**这些层的 Δθ 是精确的 0**。

这给了一道**已知答案的考题**：换回 8–15 层必须毫无变化，显著的恢复必须全落在 0–7 层。判据是二值的、不用挑阈值，比"峰值落在某个区间"灵敏得多。**扫不出来就说明整条 pipeline 有问题，不能接着扫其他方法。**

RMU_downproj（只动 5–7 层的三个 `down_proj`）是更完整的版本：13 层已知为零（特异性）+ 3 个已知非零的矩阵（灵敏度），两头都有已知答案。默认 RMU 只有特异性那一半。

**② 操作点对齐**

默认配置下所有方法都是 lr 1e-5 跑 10 epoch。GA 在第 10 个 epoch 已经崩了，NPO 可能刚开始遗忘。此时比较任何参数量，比的是"走了多远"，不是"改得准不准"。

判据必须**在看到结果之前定死**（retain model 逐样本 ROUGE 的 bootstrap 95% 上界 + utility 80% 底线），否则就是事后挑点，所有跨方法比较失去意义。arXiv 2509.26427 讲的正是这件事：ascent 类方法对学习率和时长极度敏感、缺少停止准则，看完最终指标再回头挑最好的 run 本身就是有偏的。

**③ 逐层 $\|\Delta\theta\|$ 对照**

没有它，restoration 的结论是平凡的：

- 恢复量完全跟着 $\|\Delta\theta\|$ 走 → "改得最多的层最重要"，没有信息量
- 有层 $\Delta\theta$ 不大但一换回来知识就回来 → **真发现**
- 全换回来才回来 → 知识是分布式的，没有单点

RMU 在这张图上是最干净的正面例子：$\Delta\theta$ 散在 0–7 层，但因果有明确边界。

---

## 遗忘方法全景

| 类别 | 干了什么 | 代表 |
|---|---|---|
| 重训（黄金标准） | 把 forget 数据拿掉从头再训 | retain model —— 只用来当尺子 |
| **梯度类** | 在 forget 上反着走，把 loss 推高 | **GA**、**GD** |
| **偏好类** | 借对齐算法的形式，压低模型对 forget 答案的偏好 | **NPO**、SimNPO、**DPO-idk** |
| **表征工程类** | 不管输出，直接把某一层的内部激活搞乱 | **RMU**、Circuit Breakers、RepNoise |
| 定位-编辑类 | 先找到知识存在哪些权重，只改那里 | SalUn、任务向量取负、ROME/MEMIT 系 |
| 不改权重的 | 提示词、护栏、推理时拦截 | 严格说不算遗忘 |

**本轮跑的五个：**

- **GA** — 在 forget 上做梯度上升让模型答错。完全不管 retain，跑久了整个崩掉。最朴素也最容易失败
- **GD** — GA + 同一步里在 retain 上正常训练，两个梯度相加。用 retain 拉住它别崩
- **NPO** — 不无限推高 loss，而是"相对一个冻结的参考模型，降低 forget 的偏好"。有饱和上限，比 GA 稳得多
- **DPO-idk** — 把 forget 的答案换成"我不知道"再做偏好优化。**本质是教模型闭嘴，不是删知识**，所以天然是"遗忘不足"的典型样本
- **RMU** — 把 forget 样本在第 7 层的激活推向随机方向，同时要求 retain 样本在该层激活与原模型一致。**因为它明确知道自己动了哪一层，所以是阳性对照**

**一句可以放进 motivation 的话**：定位-编辑类方法（尤其 SalUn）的**前提假设**，正好是本实验要验证的东西——"知识集中在少数权重/层上，所以只改那里就够了"。如果测出来是分布式的，那就是对这一整类方法的反驳。

---

## 分段排期

**前缀式：每段停下来都是完整的。**

| 段 | 内容 | 累计 | 停在这里能讲什么 |
|---|---|---|---|
| **A** | 环境 → Gate 1a → 训 5 个 run → NPO/RMU/DPO 的操作点 → restore.py + 两个等式测试 → Gate 2(RMU) → 扫层 → top-1 移植 → 4 张图 → 8 页报告 | **~20 h** | 三种机制的压制点定位，**带阳性对照**，可信 |
| **B** | GA、GD 的操作点与扫层 | +6 h | 跨族趋势（师兄明确要的） |
| **C** | RMU_downproj（Gate 2 的灵敏度那一半） | +4 h | 阳性对照两头都有已知答案 |
| **D** | Fisher Overlap + 与恢复量叠图 | +4 h | 相关性 vs 因果 |
| **E** | top-3 层全套指标 + attn/mlp 模块细分 | +5 h | 层内定位 |
| **F** | 单方法 relearning 曲线 | +3 h | "40 条数据 N 步全回来了" |
| | 报告扩写 | +5 h | |

**全做约 47 小时**（6–7 个工作日）。GPU 净跑约 25 小时，其余是人工。

**两处顺序不能调**：Gate 2 必须在扫其他方法之前（gate 排在被 gate 的东西后面等于没 gate）；两个精确等式测试必须在 restoration 放全量之前（跳过省 20 分钟，返工要 4 小时）。

**段 D 值不值 4 小时**：值，因为两种结果都是结论。Fisher Overlap 和 Restoration 是同一个问题的两种答案——前者是相关性（"这些模块对 forget 和 retain 都重要，所以难办"），后者是因果（"就是这几层压住了知识"）。两条曲线对得上 → "Fisher 能预测因果位置，可以当便宜的代理"，正面接住师兄那句"fisher 就是一个类似的工具"；对不上 → "参数重要性 ≠ 因果压制点"，这恰恰说明为什么必须做干预而不能只做测量，是整个工作的立论。

---

## 两个失败模式对照

| | 灾难性遗忘 | 遗忘不足 |
|---|---|---|
| 现象 | 模型能力崩塌、输出乱码 | 看起来忘了，实际知识还在 |
| 行为侧指标 | `model_utility` 掉（含 retain / real_authors / world_facts 三个分项区分局部损伤与整体崩塌） | `extraction_strength`、`exact_memorization`、`mia_min_k_plus_plus`、`privleak` 仍然高 |
| 参数侧证据 | 受损模块的 $\|\Delta\theta\|$ 大且落在 Fisher Overlap 高的地方 | 压制点集中、$\|\Delta\theta\|$ 小 |
| **干预证据（主）** | **换回受损层能修回 utility** | **换回某层知识就回来** |
| 补充 | — | relearning：几步就弹回来（段 F） |

**`forget_quality` 单独看，它和 ES/MIA 打架时以后者为准。** 前者是与 retain model 的 KS 检验，后者是直接攻击。

**Layer Restoration 是唯一两边都能给因果证据的工具**——这是把它排在第一优先级的理由，也是砍掉 Fisher 的 $D$/$C$ 之后这张表仍然成立的原因：换回一层时既看 forget 回不回来，也看 utility 修不修得回，一次评测同时覆盖两轴。

---

## 报告里要主动交代的局限

先说比被问出来好：

1. **单 seed**。默认 `seed: 0`，每方法各跑一次。方法间的小差异可能是噪声，只讨论跨族的大趋势，不解读同族内的细微差别。
2. **五个方法，preference 族只有 NPO 和 DPO-idk**（SimNPO 砍了），族内对照薄。
3. **超参未调**。所有方法都用库默认（lr 1e-5、10 epoch），操作点对齐部分缓解，但不等于每个方法都在自己最好的状态。
4. **DPO 与 NPO 数据设定不同**（idk vs 原答案），机制是输出替换 vs 偏好压低，同族比较不完全对等。"DPO 和 NPO 差很远"不一定是发现。
5. **单卡训练，与 `repro.md` 的 2×L40s + ZeRO-3 不同**。有效超参一致，但库作者明确说过分布式设置会影响结果，所以绝对数值不与 repro.md 对齐，只在内部横向比较。这也是 Gate 1b 只能当软 gate 的原因。
6. **TOFU 的 forget/retain 只是微调数据**，预训练语料动不了，所以"删干净"在这里是相对 retain model 而言的。
7. **操作点判据是预注册的**（bootstrap 95% 上界 + utility 80% 底线），在看到结果之前定死。这是刻意的：事后挑操作点会让所有跨方法比较失去意义。80%/90% 两档一并报告。
8. **操作点粒度是 10 步**（`save_steps=10`，共 12 个 ckpt，约占训练全程的 8%）。这是磁盘约束的结果：85 G 卷放不下更密的快照（`save_steps=4` 是 30 个 ckpt = 75 G）。粒度粗意味着选中的 A 更深入遗忘，恢复量的绝对值会跟着偏大；跨方法比恢复量的前提是各方法 overshoot 相当。**每张恢复量的图都标注了该方法的 overshoot**，可比性由读图者自行判断。此外，遗忘极快的方法（第 10 步之前就越线者）无法进一步定位，其 A 的 overshoot 会显著偏大——若 GA 之外还有方法落入这一类，相关比较需打折解读。
9. **relearning 只有一个方法一条曲线**（段 F），是示例不是趋势；其余方法的遗忘不足证据来自 ES/MIA 和 Restoration，这两者测不到"攻击者拿到模型后还能再微调"这个维度。
10. **empirical Fisher**，不是 true Fisher（若做了段 D）。
11. **RMU_downproj 若调过 lr**，它只是 pipeline 的阳性对照，不进跨方法比较；报告里注明所用 lr。

---

## 汇报口径

**不要把 pilot 讲成完整研究**，会被一眼看穿。讲成这样：

> "我先用 20 小时把整条因果分析流程跑通了，**包括阳性对照**——RMU 的第 8–15 层参数在数学上必须是零，实测确实是零，且换回去毫无变化，说明 restoration 这套方法本身是可信的。在这个前提下，NPO / RMU / DPO 三种不同机制的压制点分别落在 ___。
> 这是 pilot。扩到五个方法 + Fisher Overlap 对照 + relearning 曲线，需要再 ___ 小时。"

三个好处：先证明工具可信再报结论（这正是 Restoration 排第 2 的原因）；明确说了是 pilot（不会被追问单 seed 和方法数）；给出扩展路径（他可以直接说"那就扩"或者"够了"）。

**关于 Activation Patching**（他排第 1，我们不做）：**不要默默略过**。明确写——Layer Restoration 是 Activation Patching 的权重空间粗粒度版本，先用它把范围定位到层，Patching 做层 × token 的细分辨率，列为下一步；理由是跨模型搬激活在 GA 这类跑崩的模型上是分布外的。这样你的第 2 变成他第 1 的前置步骤，而不是绕过他的建议。

**关于 Tuned Lens / CKA / PCA**（他的表里有）：师兄自己已经做过表征分析这块，避让，不碰。

**每张图从段 A 开始就存好，别等最后一天补。**

---
---

# 术语表

## 数据与模型

**unlearning（遗忘学习）** — 让训练好的模型「忘掉」指定的一批数据，同时保住其他能力。动机是隐私合规（被遗忘权）、版权、有害内容清除。

**forget set / retain set / holdout set** — 要忘的、要保的、以及模型从没见过的第三份。MIA 靠 holdout 做对照：模型对 forget 的行为和对 holdout 一样，说明真忘了。

**TOFU** — 遗忘学习基准。200 个**虚构**作者 × 每人 20 条问答 = 4000 条。用虚构人物是为了确保这些知识只来自微调、不来自预训练，才有干净的对照。

**forget10 / retain90 / holdout10** — forget10 是要遗忘的 10%（400 条），retain90 是剩下的 3600 条。两者相加是 TOFU 的全部，但**不是模型训练数据的全部**——还有万亿级预训练语料你碰不到。

**target model** — 在 forget + retain 全集上微调过的模型，「什么都记得」的起点。

**retain model** — 只在 retain 上微调、从没见过 forget 数据。**黄金标准**：理想的遗忘结果应该和它一样。`forget_quality` 就是拿遗忘后模型和它做统计检验。

**操作点（operating point）** — 拿来做跨方法比较的那个统一状态。不同方法在同样 lr 和 epoch 下会停在遗忘-效用权衡曲线的完全不同位置，不先对齐，任何跨方法比较都在比「走了多远」而不是「走得准不准」。

## 评测指标

**forget_quality** — 把遗忘后模型和 retain model 的 truth ratio 分布做 KS 检验。有黄金标准做锚点，这是它比 NLL 强的地方。

**model_utility** — 由 retain、real_authors、world_facts 三个分项聚合（每项各有 probability、ROUGE、truth ratio，共九个取调和平均）。

**extraction_strength（ES）** — 给一段前缀，模型能不能续出剩下的内容。测的是**实际生成**，不是 teacher forcing 下的概率。

**exact_memorization（EM）** — 逐 token 精确记住了多少。

**MIA / MinK++** — 判断某条数据有没有参与过训练。攻击成功 = 知识还在。MinK++ 是最强的变体之一且 **reference-free**——用 NLL 报「忘掉了」，别人用 MinK++ 一测可能就把知识捞回来。**`mia_reference` 不用**：参考模型若取 retain90，就和 `forget_quality` 共用同一个锚点，这个 MIA 就不独立了。

**NLL 为什么不合格** — ① 无上界，GA 定义上就是把它推高，多跑几步能到任意大；② 没有参照点，说不了「多高算忘干净」；③ 它就是 MIA 里最弱的 LOSS attack；④ 分不清「知识删了」和「模型崩了」。

## 分析工具

**Δθ** — 遗忘前后的参数变化量，两个 `state_dict` 相减。逐层范数 $\|\Delta\theta_{\rm layer}\|$ 是 restoration 结论的必需对照。

**Layer Restoration** — 把某层参数换回原始值，看知识回不回来。**干预**，不是测量，所以能给因果结论。测**必要性**。

**层移植（transplant）** — 反方向，把遗忘后模型的某层放进原始模型。测**充分性**。必要 + 充分都成立，才是干净的压制点。

**阳性对照（positive control）** — 用一个答案已知的案例来验证方法本身管不管用。这里就是 RMU。

**Fisher information** — 参数重要性的一种算法：$I_i = \mathbb{E}[(\partial L/\partial\theta_i)^2]$，**per-sample** 梯度平方在数据集上求平均（不是 batch 平均后再平方，那差约 $B$ 倍且方差被抹掉）。喂 forget 得 $I_f$，喂 retain 得 $I_r$。出处是持续学习里的 EWC（Kirkpatrick et al., 2017）。

**Fisher 的价值** — 严格的参数重要性应该用 Hessian，但它是 $n\times n$ 的，1B 模型算不动。**在最优点附近 Fisher 近似等于 Hessian 的对角线，而只需要梯度平方：二阶的信息，一阶的代价。** 代价是这个近似只在模型拟合得好、$\Delta\theta$ 小的时候成立——对 GA 这类崩掉的方法最不可信。

**Fisher Overlap** — 各自归一化到和为 1 后取 $\sum_i\min(\hat I_f,\hat I_r)$。重叠高的参数最难办：改了伤模型，不改忘不掉。

**empirical vs true Fisher** — 用数据集真实标签算的是 empirical；true Fisher 要从模型自己的预测分布里采样标签。实践中都用前者，但近似 Hessian 的条件更严。**报告里要写清用的是哪个。**

## 本轮不做，但可能被问到

**梯度冲突** — forget 梯度和 retain 梯度的余弦。负值 = 两个目标直接对抗。**局限**：只能诊断灾难性遗忘——一个什么都不做的方法冲突为零、模型完好、知识一点没少，这个指标给它满分。这也是师兄否掉它作主方向的原因。

**Activation Patching** — 模型运行时在某层某个 token 位置，把遗忘后模型的中间结果替换成原模型的。分辨率是「第几层 × 第几个词」，因果力度最强但工程量最大（挂 forward hook、跑 $L\times T$ 次前向）。**陷阱**：标准做法是同模型不同输入，这里是跨模型；两个模型表征漂移太远的话，搬过去的激活值就是分布外噪声，GA 跑崩的模型尤其危险。

**influence function** — Fisher 的严格版，问「某条训练样本对模型的贡献有多大」。形式是 $-H^{-1}\nabla_\theta L(z)$，要算 Hessian 的逆，LLM 上做不动。写论文时可作理论出处引用。

**CKA / PCA / SVCCA / Tuned Lens / Probe** — 表征分析手法，输出通常是「横轴层号」的图。**师兄已经做过这块，避让。**

---

## 参考

- **OpenUnlearning 技术报告** — arXiv 2506.12618。框架设计、指标 meta-evaluation、relearning 与 quantization stress test 的官方设定。
- **DA 类遗忘方法的失败分析** — arXiv 2509.26427。forget/retain 之间的数据依赖导致 ascent 类方法系统性失败；关于停止准则缺失和超参敏感性的论证，是「操作点对齐」的直接依据。
- **EWC** — Kirkpatrick et al., 2017。Fisher 作为参数重要性度量的出处。
- **TOFU** — Maini et al., 2024。基准本身。
