# 遗忘失败分析 — 实验设计 v3.1

**一句话目标**：不是比较哪个遗忘方法效果好，而是搞清楚它们**为什么失败**，并且用因果证据而不只是相关性来支撑。

失败分两种，两种都要能诊断：

- **灾难性遗忘** — 模型被搞坏了，通用能力崩塌
- **遗忘不足** — 模型看起来忘了，但知识还在，稍加微调就回来

---

## 文档分工

从 v3 起拆成三份，不要在两处维护同一个数值或同一条命令。

| 文件 | 内容 | 读者 |
|---|---|---|
| 本文件 | 设计理由、判据的来由、优先级与砍取、局限、术语表 | 人 |
| `RUNBOOK.md` | 命令、产物契约、STOP-ASK、幂等约定 | 执行（人或 agent） |
| `scripts/constants.yaml` | 所有阈值和常数 | 两者共同引用 |

**阶段 0–1 的命令保留在本文件**，因为那一段是人工 debug，失败模式太杂写不成判定式。**阶段 2 往后的命令只在 RUNBOOK 里**。

---

## v3 / v3.1 相对 v2 的改动（11–15 为 v3.1）

| # | 改了什么 | 为什么 |
|---|---|---|
| 1 | 拆出 RUNBOOK 和 constants.yaml | 两份文档各自带命令必然漂移 |
| 2 | repro 校验拆成 Gate 1a（硬）/ 1b（软） | `repro.md` 明写单卡结果会变，原判据会误触发 |
| 3 | 操作点给出可执行判据（bootstrap 95% 上界） | v2 只写"降到 retain 水平"，没有容差，等于看完结果再定 |
| 4 | utility 底线改 80% 主 / 90% 复核 | 对齐 OpenUnlearning 论文的 MU filter 口径 |
| 5 | `save_steps=4` + 两段式评测 | epoch 粒度太粗，越线点可能落在 epoch 之间 |
| 6 | RMU 阳性对照提到阶段 3 第一步，判据改二值 | gate 排在被 gate 的东西后面等于没 gate |
| 7 | 指标清单修正：MIA 默认没开 | v2 让你去看 `TOFU_EVAL.json` 里根本不存在的字段 |
| 8 | `mia_reference` 砍掉 | 参考模型用 retain90 会与 `forget_quality` 循环 |
| 9 | 梯度冲突改离线在 ckpt 上算 | 不进训练循环，不改库，省 50% 训练时间 |
| 10 | 恢复量显著性用 bootstrap CI | 数值离散度预期为 0，撑不起"是否显著" |
| 11 | Gate 2 加灵敏度条款；RMU_downproj 并入 Gate 2、排在扫层之前 | 原判据全是"零处为零"，能空过；变体排在扫层之后等于没 gate |
| 12 | 评测配置拆三份：full / oppoint / cheap | 只有 full 时"精简评测"没有命令，agent 会跑全套或自己猜 |
| 13 | 步数、ckpt 数改为 Gate 1b 后从 `trainer_state.json` 回填 | 12.5 步/epoch 的余数处理随 transformers 版本变，写死会错 |
| 14 | relearning 参数与恢复速度定义补齐 | 验证二的 y 轴此前没定义 |
| 15 | S7 keep 列表加 GD 粗评 ckpt；不可行方法也算 Δθ | 原流程会先删掉 S10 要的 ckpt；GA 那个点是验证一的证据 |

---

## 优先级（排期挤压时按这个砍）

阶段 2 的 Fisher 和阶段 4 的 relearning 曲线，正好是师兄两篇论文的核心工具。**差异化几乎全押在阶段 3（RMU 阳性对照 + Layer Restoration + 充分性移植）。**

1. **保**：阶段 1（Gate + 操作点）→ 阶段 3（干预证据）→ 阶段 5（闭环）
2. **保最小版**：阶段 4 relearning 缩到 2 个方法
3. **压缩**：阶段 2 只留 Fisher Overlap 和归一化后的 $D$、$C$，不做梯度冲突
4. **砍**：3B 全量、Activation Patching

**为什么 relearning 不能整个砍。** 遗忘不足那一轴的行为侧证据并不是只有它——`extraction_strength` 和 MIA 本来就是行为侧的，Layer Restoration 给的还是更强的干预证据。真正丢掉的是**对抗性维度**：ES/MIA 在权重固定下测，Restoration 是你自己动权重，只有 relearning 让**新的梯度更新**去捞知识，对应"攻击者拿到模型后还能再微调"这个威胁模型。

**依赖关系**：阶段 5 的验证二以 relearning 恢复速度为 y 轴，所以 relearning 必须排在阶段 5 之前。真跑不动时验证二有免费替代，见阶段 5。

---

## 阶段 0 — 环境准备（人工）

```bash
conda create -n unlearning python=3.11
conda activate unlearning
pip install ".[lm-eval]"
pip install --no-build-isolation flash-attn==2.6.3

python setup_data.py --eval_logs   # retain / target 模型的评测日志
python setup_data.py --idk         # DPO 需要 data/idk.jsonl，漏了直接崩
```

`--eval` 也能用（argparse 前缀匹配到 `--eval_logs`），但 `--idk` 是独立的一项，README 的 Quickstart 里没写。

**版本**：`requirements.txt` 钉的是 `transformers==4.51.3`。`src/trainer/base.py` 顶上那句 "Modified from v4.45.1" 只是说明代码抄自哪个版本，**不要照它降版**。

**三处必改的配置**

1. **`save_strategy` 默认是 `'no'`** —— 训练全程一个中间 ckpt 都不存。设 `save_strategy=steps save_steps=4`。
   - forget10 = 400 条、有效 batch 32 = 12.5 步/epoch。名义上 12 步/epoch、120 步、30 个 ckpt，但 transformers ≥ 4.46 会把 epoch 末剩下的 4 个 micro-batch 凑成第 13 步，实际很可能是 130 步、32 个 ckpt，"每第 3 个正好落在 epoch 边界"随之不成立。**不要写死**：Gate 1b 跑完从 `trainer_state.json` 读 `max_steps` 回填 `constants.yaml` 的 `training.*`（RUNBOOK S4）。
   - `save_only_model` 默认已是 `True`，只存权重。1B 单方法峰值约 80 GB，选完操作点立刻清理（清理流程见 RUNBOOK S7，需人工确认）。盘紧就把 `save_steps` 调到 6。

2. **有效 batch 必须等于 32**。库的默认 `per_device=8 × accum=4` 单卡就是 32；24G 卡上可能爆显存，改 `per_device=4 accum=8`。**不要把官方脚本的 `per_device=4 accum=4` 抄到单卡**，那是给 2 卡用的。
   - `repro.md` 的超参表写"8 per device × 4 accum"，`tofu_unlearn.sh` 写的是"4 × 4 × 2 卡"。两份自相矛盾，但结论一致：有效 batch 32。

3. **`eval_strategy` 设 `no`**。默认是 `epoch`，训练中会跑完整 TOFU 评测，很慢；而且它只在单进程下生效（`src/trainer/base.py` 里 `num_processes != 1` 直接返回空，只 warning）。评测统一离线做，可控性更好。

**目录结构**

```
unlearning-analysis/
├── open-unlearning/    # git clone，只读
├── scripts/            # 自写代码、constants.yaml、configs/
├── results/            # 产物，契约见 RUNBOOK §1
└── notes/
```

所有命令以 `open-unlearning/` 为工作目录（`src/train.py`、`saves/` 都相对它），自写产物一律写 `../results/`——写成 `results/` 会落进库目录。`saves/` 是库的训练输出，在库目录内，是"不改库"的唯一例外。

---

## 阶段 1 — Gate 与操作点

### Gate 1a：target model 自评（硬停）

不训练，直接评 target model，和 `setup_data.py --eval_logs` 下载的同名模型日志比对。同模型、同评测代码，偏差只应来自硬件和 batch 组成。

```bash
python src/eval.py --config-name=eval.yaml \
  experiment=eval/tofu/default \
  model=Llama-3.2-1B-Instruct \
  model.model_args.pretrained_model_name_or_path=open-unlearning/tofu_Llama-3.2-1B-Instruct_full \
  forget_split=forget10 holdout_split=holdout10 \
  retain_logs_path=saves/eval/tofu_Llama-3.2-1B-Instruct_retain90/TOFU_EVAL.json \
  paths.output_dir=results/eval/target_selfcheck \
  task_name=target_selfcheck
```

容差：概率类和 truth ratio 类 ±0.01；ROUGE 类 ±0.02。ROUGE 放宽是因为**贪心解码跨硬件会翻 argmax**，400 条平均下来能差过 0.01。卡在边缘不自动放行，先 diff 逐样本生成再人工定性。

这是唯一干净的环境校验：零训练成本，任何超限都是真问题。

### Gate 1b：NPO 单方法对 repro.md（软）

`repro.md` 的硬件是 **2 × L40s + DeepSpeed ZeRO-3**，并且明确警告：改变分布式设置（**包括单卡训练**）结果会变，还点名 SimNPO 和 RMU 尤其敏感。

所以这一步**不能当硬 gate**。判据：

- `forget_Q_A_ROUGE` 或 `model_utility` 差超过 **0.15** → 硬停。这个量级是配错了，不是分布式噪声。
- 以内 → 记录到 `notes/`，继续。
- `forget_quality` **不设判据**。它是 KS 检验 p 值，本身就能差几个数量级，设判据只会频繁误报。

用**默认评测配置**评**最终权重**对 repro.md（repro 的数就是这么来的）；操作点候选评测归 S6、用 `tofu_oppoint`。这一跑同时用来回填真实步数（见阶段 0）。

### 六个方法与各自的配置

| 族 | 方法 | `experiment` | 注意 |
|---|---|---|---|
| 无约束 | GA | `unlearn/tofu/default` | |
| 带 retain 项 | GD | `unlearn/tofu/default` | 唯一测梯度冲突的方法 |
| preference | NPO | `unlearn/tofu/default` | Gate 1b 已跑，复用 |
| preference | SimNPO | `unlearn/tofu/default` | 不在官方脚本里，repro.md 无参考值 |
| preference | DPO | **`unlearn/tofu/idk`** | 需 `data/idk.jsonl`；idk.yaml 的 `override /model` 写的是 3B，必须显式传 1B |
| representation engineering | RMU | `unlearn/tofu/default` | 阳性对照 ①，见阶段 3；另有 RMU_downproj 作阳性对照 ②，不算第七个方法 |

同族表现应当接近，这是可验证的观察点。但要小心一个混淆：**DPO 走 idk 数据（答案换成"我不知道"），机制是输出替换**，而 NPO/SimNPO 是偏好压低。"DPO 和 NPO 差很远"不一定是发现，可能只是数据设定不同，报告里要讲清楚。

### 操作点对齐

默认配置下所有方法都是 lr 1e-5 跑 10 epoch。GA 在第 10 个 epoch 已经崩了，NPO 可能刚开始遗忘。此时比较 $D$ 和 $C$，比的是"走了多远"，不是"改得准不准"。

项目里那篇 [arXiv 2509.26427] 讲的正是这件事：ascent 类方法对学习率和时长极度敏感，且缺少停止准则；看完最终指标再回头挑最好的 run，本身就是有偏的。

**判据**（阈值全部在 `constants.yaml`）：

- 阈值 = retain model 逐样本 `forget_Q_A_ROUGE` 的 **bootstrap 95% CI 上界**。`TOFU_EVAL.json` 里每个指标都带 `value_by_index`，逐样本分数是现成的。
- **操作点 A** = 首个 `forget_Q_A_ROUGE ≤ 上界` 的 ckpt。
- **操作点 B** = `model_utility ≥ target 自身 model_utility × 80%` 的最后一个 ckpt。80% 对齐 OpenUnlearning 论文的 MU filter 口径；90% 作敏感性复核。
- 基线是 target model **自己**的 `model_utility`，Gate 1a 那一跑就已经算出来了。

**为什么是 bootstrap 而不是直接比点估计**：retain model 的值本身是 400 条上的点估计，10 个 ckpt 的轨迹还有噪声，"首次降到"可能触发在一次抖动上。而且判据要在看数据之前定死，否则就是事后挑点。

**两段式评测**：先评每第 3 个 ckpt（≈ epoch 边界）加最终权重，定位越线区间，再评区间内的 ckpt（`save_steps=4` 时 2 个）。每方法约 12–13 次。第一个粗评点已经越线时，区间是 [target, 第一个粗评点]。

**overshoot**：记录"上界 − 选中 ckpt 的 ROUGE"。超过 0.1 说明这个方法在两个 ckpt 之间冲过头了，**只标记不重跑**——重跑是整跑一遍，而且新旧两条轨迹不是同一条，混用会出问题。

**可行集为空**：有的方法（大概率是 GA）在 forget 降下来之前 utility 就先崩了。判定式：`feasible = model_utility(A) ≥ target 的 model_utility × 80%`；A 不存在（到最终权重仍未越线）同样不可行。这本身就是"失败"的一个干净定义，该方法退出跨方法参数比较的统计，但 Δθ 照算、在验证一里用空心点画上——它正是"D 在崩溃处最不可信"的证据。

### 指标清单

**默认配置里 MIA 是注释掉的**，这是 v2 的一处硬错。默认开启的只有：`forget_Truth_Ratio`、`forget_quality`、`forget_Q_A_Prob`、`forget_Q_A_ROUGE`、`model_utility`、`privleak`、`extraction_strength`。

其中 `privleak` 内部已经跑了 `mia_min_k`（配置里 `defaults: - .@pre_compute.mia_min_k`），拿 forget 和 holdout 比、再对 retain 日志校准。所以默认配置下你已经有一个 MIA 派生指标。

**Gate 1a/1b 用默认配置**（repro 的数就是默认配置跑出来的）。**通过之后再开两项**：`exact_memorization` 和 `mia_min_k_plus_plus`。这两个只有 `batch_size`、没有 generation config，是纯前向，边际成本很小；而 MinK++ 比 privleak 用的 MinK 强一档，你的核心判据之一是"`forget_quality` 和 MIA 打架时以 MIA 为准"，判据本身用弱攻击会让结论偏软。

**`mia_reference` 不开**。它要额外配参考模型，而参考模型若用 retain90，就和 `forget_quality` 共用同一个锚点，这个 MIA 就不独立了——恰好破坏它在你论证里的作用。MinK++ 是 reference-free 的，没这个问题。

开启方式见 RUNBOOK S3。评测配置一共三份：`tofu_full`（全套）、`tofu_oppoint`（`forget_Q_A_ROUGE` + `model_utility`，定操作点用）、`tofu_cheap`（`forget_Q_A_ROUGE` + `extraction_strength`，扫层 / 移植 / relearning 用）。只做 full 的话"精简评测"就没有命令，agent 会每次跑全套。**都不要沿用 `tofu.yaml` 这个名字**（同名会 shadow 掉库里的文件，命令行看不出加载的是哪份）。

**两轴对应的指标**

- 灾难性遗忘：`model_utility` 为主，`retain_Q_A_ROUGE` / `real_authors_Q_A_ROUGE` / `world_facts_Q_A_ROUGE` 三个分项区分局部损伤和整体崩塌
- 遗忘不足：`forget_Q_A_ROUGE`、`extraction_strength`、`exact_memorization`、`mia_min_k_plus_plus`、`privleak`

`forget_quality` 单独看，**它和 ES/MIA 打架时以后者为准**。

---

## 阶段 2 — 参数侧测量

作用不是结论，是给阶段 3 提供输入和对照量。全部在操作点 A 上做。执行细节见 RUNBOOK S8–S10。

### Fisher

**必须在遗忘之前算**，用原始 target model。Fisher 近似 Hessian 只在最优点附近成立，跑崩的模型不满足。

**`batch_size` 必须是 1。** Fisher 要的是 per-sample 梯度平方的期望；batch > 1 时累加的是"batch 平均梯度的平方"，两者差约 $B$ 倍，样本间方差还被平均掉了——算出来的不是 Fisher。这是 v1 代码里的实际 bug。

$I_f$ 用 forget10 全部 400 条，$I_r$ 从 retain90 随机抽 400 条保持对称。用真实标签算出来的是 **empirical Fisher**，报告里要写明。

Fisher Overlap：各自归一化到和为 1 后取 $\sum_i \min(\hat I_f, \hat I_r)$，聚合到模块级。重叠高的参数最难办——改了伤模型，不改忘不掉。

### Δθ 定位精度

$D = \sum_i I_r(i)\Delta\theta_i^2$、$C = \sum_i I_f(i)\Delta\theta_i^2 / \sum_i I_f(i)$。

**但这两个量不能直接跨方法比**：都随 $\|\Delta\theta\|^2$ 增长，所以走得远的方法 $D$ 大、$C$ 也大——那是"改得多"不是"改得准"。

对照：生成每模块范数与真实 $\Delta\theta$ 相同、方向随机的扰动，算 $D_{\text{rand}}$、$C_{\text{rand}}$，**上报 $D/D_{\text{rand}}$ 和 $C/C_{\text{rand}}$**。大于 1 才说明确实打在了重要参数上，等于 1 说明和乱改一个方向没区别。

$D$ 小、$C$ 大是理想状态。$D$ 有二阶近似撑着，$C$ 只是必要条件——参数改了不等于知识删了，要靠阶段 3、4 补。

### 梯度冲突（仅 GD）

师兄已否掉这个作主方向，所以只留最小成本版本：**离线在 ckpt 上算，不进训练循环、不改 trainer**。对 GD 的 10 个 epoch 边界 ckpt 各算一次 $g_f$、$g_r$ 的余弦。

注意这里要的是**均值梯度**，所以大 batch 是正确的——和 Fisher 那条 `batch_size=1` 的要求正好相反，别混。结果标注为**全 batch 梯度冲突**，不是逐步 minibatch 冲突。

---

## 阶段 3 — 干预证据（主证据）

**这是两周的重点，也是相对师兄已有工作的主要差异化。**

工程上就是 `state_dict` 替换 + 重跑 eval。Llama-3.2-1B 有 16 层。

**为什么比阶段 2 强**：阶段 2 是测量，只能说"这层相关"；干预能说"就是这层"。

### 3a — RMU 阳性对照（gate，必须先做）

`configs/trainer/RMU.yaml` 里 `module_regex: model\.layers\.7`。查 `src/trainer/unlearn/rmu.py` 确认：forget loss 是层 7 输出激活对 control vector 的 MSE，retain loss 是 `EMBED_DIFF`（层 7 激活对参考模型激活的 MSE）。`forward_with_cache` 虽然跑完整前向，但 **loss 的计算图止于第 7 层**。

所以 8–15 层的 `.grad` 是 `None`，AdamW 整个跳过它们——**Δθ 是精确的 0**，`weight_decay` 也不作用（PyTorch 只对 grad 非 None 的参数施加 decay）。

**判据是二值的，不用挑阈值**：

> 8–15 层 Δθ 严格为 0，且这些层的 restoration 恢复量为 0；显著的恢复全部落在 ≤7 层；**且 ≤7 层里至少有 1 层显著**。

前三条是特异性（零的地方必须为零），第四条是灵敏度——没有它，一个什么都检测不到的 pipeline 也能三条全过，gate 是空的。这是个阶跃函数。恢复量只要泄漏到第 8 层以上，就是 restoration 代码有 bug 或评测噪声底太高——比"峰值落在某个区间"灵敏得多。**不通过就停，不要接着扫其余五个方法。**

**峰值层只记录，不作判据。** 配置里 `trainable_params_regex` 是 `.*`（代码默认才是那三个 `down_proj`），所以 0–7 层全都会更新，包括 embedding，峰值位置没有理论预期。

**顺带的确定性检查**：8–15 层的 8 次 restoration 跑的是同一个模型（换回去等于没换）。贪心解码 + 固定 batch，预期 8 次结果逐位相同、离散度为 0。非 0 则说明评测本身不确定，影响所有后续比较，要上报。

**第二个对照 RMU_downproj**（不是可选）：把 `trainable_params_regex` 覆写成 `model\.layers\.(5|6|7)\.mlp\.down_proj\.weight`（RMU 原论文做法，配置里注释着），只动三个权重矩阵。它比默认 RMU 更完整：13 层 Δθ 严格为 0（特异性）+ 3 个已知非零的矩阵集中在 5–7 层（灵敏度），两头都有已知答案；默认 RMU 只有特异性那一半。训练顺序 NPO → RMU → RMU_downproj → 其余，**两个对照都在扫层之前过 gate**——排在扫层之后就犯了改动表 #6 那条错。前提是它得真的到操作点 A：默认 lr 到不了允许只对这一跑调 lr（它不参与跨方法比较），仍到不了就记录后跳过，Gate 2 只剩 RMU 那一半。**regex 里的 `|` 不要加反斜杠**：`\|` 在 Python re 里是字面竖线，模式会匹配不到任何参数，训练等于没动，会被误判成"到不了操作点 A"；训练前打印可训练参数名，必须恰好 3 个。

**两个免费的精确等式测试**（RUNBOOK §0.4）：把所有 Δθ≠0 的层全部换回 target，结果必须和 target 自评逐位相同；把所有 Δθ≠0 的层全部移植进 target，结果必须和遗忘后模型自评逐位相同。自动判定、不需要人看中间量，restore / transplant 脚本过了它们才准放全量。

### 3b — 必要性扫描

Gate 通过后全扫 16 层 × 每个可行方法。不做收窄——1B 只有 16 层，便宜指标一次 2–4 分钟，全扫约 5 GPU 小时，收窄是假节约。

**必须同时记录每层的 $\|\Delta\theta_{\text{layer}}\|$，并把恢复量对它作图。** 没有这个对照，结论是平凡的：换回某层知识回来了，很可能只是因为那层改动最大。

- 恢复量完全跟着 $\|\Delta\theta\|$ 走 → "改得最多的层最重要"，没有信息量
- 有层 $\Delta\theta$ 不大但一换回来知识就回来 → 真发现
- 全换回来才回来 → 知识是分布式的，没有单点

RMU 在这张图上是最干净的正面例子：$\Delta\theta$ 散在 0–7 层，但因果有明确边界。

**显著性**：对逐样本（配对）恢复量做 bootstrap，CI 下界 > 0 才算显著；每个方法 16 次检验，CI 水平按 Bonferroni 取 1 − 0.05/16，否则 95% CI 在 96 次检验里平均会出约 5 个假阳性。不用上面那个确定性离散度——它预期就是 0，撑不起判断。

### 3c — 充分性移植

反方向：把遗忘后模型的某层移植进 **target model**，其余不动，看遗忘效果转移多少。只对 3b 选出的 top-k 层做（不全扫），因果结论强一档。

3b 测必要性，3c 测充分性。两者都成立的层才是干净的压制点。

---

## 阶段 4 — Relearning（保最小版，必须在阶段 5 之前）

**库里已有 relearning stress test**，不用从头写。但官方设定（完整 forget10 微调 1 epoch，lr 2e-5）有问题：在全部 forget 数据上微调、再在同样数据上测，测的是"重新背下来"。

**改法**：在 forget10 的 10% 子集（40 条）上微调，在剩余 360 条 held-out 上评测。这样恢复才说明是知识残留。

- 用 forget 数据微调 → 测遗忘不足，几步就弹回来说明知识从来没走
- 用 retain 数据微调 → 测损伤深度

最小版只做 forget 子集微调；retain 微调模式不在两周内，报告里写成 next step。

**曲线形状比终点值重要**。两个方法终点一样，但一个第 5 步跳回去、另一个爬 200 步，机理完全不同。

**最小版选法**：等阶段 2 出结果，取 $C/C_{\text{rand}}$ 最高和最低的两个方法，这样散点图上的两个点是有意义的两端。阶段 2 被压缩时退回 NPO + DPO(idk)。

**不要用 GA 做这一轴的对照**：GA 不回弹很可能是模型已经坏了，不是知识被删了——这个点没法解读。GA 归验证一管。

**成本**比看起来低：微调很快（40 条几十步），开销在曲线上每隔几步要在 360 条上评测。只跑 `forget_Q_A_ROUGE` 的话一个方法约一小时量级。

**具体参数**（`constants.yaml` 的 `relearning.*`）：从操作点 A 起，batch 4 不累积（40 条 = 10 步/epoch），lr 2e-5，跑 200 步，每 10 步存一个 ckpt 离线评；评测照常跑全部 400 条，只对 360 条 held-out 的 `value_by_index` 取平均，不用自定义评测集。**恢复速度** = 首个 held-out ROUGE 回到 (target − 遗忘后) 差距 50% 的步数（200 步内没到记 >200），曲线 AUC 作备选——这就是验证二的 y 轴。

---

## 阶段 5 — 闭环验证

到这一步手上已经有：$D/D_{\text{rand}}$、实测 `model_utility` 下降、$C/C_{\text{rand}}$、relearning 恢复速度。

**验证一：$D/D_{\text{rand}}$ vs `model_utility` 下降幅度**

- 相关性好 → Fisher 工具被验证了，参数级证据能预测行为级后果
- 相关性差 → 二阶近似在大 $\Delta\theta$ 下失效。这本身也是结论，且正好和 GA 崩掉对上：$D$ 最不可信的地方，恰恰是灾难性遗忘最严重的地方

数据阶段 1–2 就有，完全免费。不可行的方法（大概率是 GA）在 A、B 两点各算一份 $D/D_{\text{rand}}$，用空心点画上、不进拟合——它正是"D 在崩溃处最不可信"的证据，单独成图等于扔掉。

**验证二：$C/C_{\text{rand}}$ vs relearning 恢复速度**

$C$ 小应该预测"几步就弹回来"。这一张依赖阶段 4，最小版的两个点也能画，只是两端对照而非趋势拟合。

**免费替代**（relearning 真跑不动时）：y 轴换 `extraction_strength` 或 `mia_min_k_plus_plus`，六个点齐全。但它更弱：ES 在单个操作点、权重固定下测，会被输出压制骗过——而输出压制恰恰是被研究的现象本身，有循环论证风险。只当兜底，且在图注里说明测不到再微调这个维度。

这两张图把参数侧和行为侧接起来了，正是最后那张对照表想要的东西。**汇报时这是最有分量的两页。**

---

## 两周排期

| 时间 | 做什么 | RUNBOOK |
|---|---|---|
| 第 1 天 | 环境、Gate 1a、三份评测配置、**三处 dry run 全部验完**、按实测耗时重算排期 | S0–S3b |
| 第 2 天 | Gate 1b（NPO）+ 步数回填 | S4 |
| 第 3–4 天 | 七个 run 训练（顺序 NPO → RMU → RMU_downproj → 其余，`save_steps=4`）+ 操作点两段式评测 + ckpt 清理 | S5–S7 |
| 第 5–6 天 | Fisher、Δθ 归一化与随机方向对照、GD 梯度冲突 | S8–S10 |
| 第 7 天 | **Gate 2：RMU + RMU_downproj 两个阳性对照**（RMU 的 S6 一完成就可提前做，不依赖 Fisher） | S11 |
| 第 8–10 天 | 扫层 → 可疑层全套评测 → 充分性移植 | S12–S14 |
| 第 11 天 | relearning 最小版（RMU_downproj 已并入 Gate 2） | S16 |
| 第 12 天 | 闭环验证，出两张散点图 | S17 |
| 第 13 天 | 补实验、统一出图 | |
| 第 14 天 | 做 PPT | |

**两处顺序不能调**：Gate 2 必须在扫层之前（否则 gate 不到任何东西）；relearning 必须在阶段 5 之前（否则验证二没有 y 轴）。

**三处 dry run 全部压到第 1 天**（`eval=<name>` 的组覆盖、`trainer=RMU_downproj` 的加载、relearning 的自建 data 配置）。它们原本分散在 S3、S5、S16，最晚一处到第 11 天才验——那时候发现搞不定就没时间改了。第 1 天末用 `notes/eval_timing.md` 的实测单次耗时把整张排期重算一遍：全流程约 84 次 `tofu_oppoint`、35 次 `tofu_full`、173 次 `tofu_cheap`，而 `tofu_oppoint` 单次要在 600 条上生成，不便宜。**要砍就在第 1 天砍**，按上面优先级那节，别等到第 8 天。

头两天大概率都在填坑（flash-attn 装不上、模型下载、Gate 1a 对不上），这一段不适合无人值守。

**关于 3B**：24G 卡跑不了 3B 全参数微调，扩到 3B 需要另租 48G 卡。两周内不做，写进给师兄的消息让他定——要的话是 NPO + RMU 两个方法的一致性验证，另加约一周和一张 48G 卡。

**汇报形式是 PPT + 可视化结果**，从阶段 2 开始每张图都存好，别等最后一天补。

---
## 坑清单

**配置类**

1. **`save_strategy` 默认是 `'no'`** → 一个中间 checkpoint 都不存，过程分析和操作点选择全部要重跑
2. **`setup_data.py --idk` 没跑** → DPO 找不到 `data/idk.jsonl`，直接崩
3. **DPO 用了 `unlearn/tofu/default`** → 数据配置不对，跑出来的不是 idk 版 DPO
4. **`idk.yaml` 里 `override /model` 写的是 3B** → 必须显式传 `model=Llama-3.2-1B-Instruct`
5. **eval 漏 `pretrained_model_name_or_path`** → 静默评测默认 target model，不报错，数字看起来正常
6. **eval 漏 `retain_logs_path`** → `forget_quality` 算不出来
7. **`holdout_split` 和 `forget_split` 不匹配** → MIA 全废
8. **把官方脚本的 `per_device=4 accum=4` 抄到单卡** → 有效 batch 变 16，对不上 repro.md
9. **多卡时指望训练中评测** → `num_processes != 1` 时评测器直接返回空，只 warning 不报错
10. **拿 repro.md 硬 gate 单卡训练结果** → repro.md 是 2×L40s + ZeRO-3 跑的，且明确警告单卡会变。硬 gate 会在第 2 天误停
11. **自定义评测配置沿用 `tofu.yaml` 这个名字** → 会 shadow 掉库里的同名文件，命令行看不出加载的是哪份
12. **照 `base.py` 注释把 transformers 降到 4.45.x** → `requirements.txt` 钉的是 4.51.3，那句注释只是说明代码抄自哪里
13. **跨 run 混用 ckpt** → GPU 训练非逐位确定，重跑出的轨迹不是同一条
14. **RMU_downproj 的 regex 给 `|` 加了反斜杠** → `\|` 是字面竖线，匹配不到任何参数，训练没动，还会被误判成"到不了操作点 A"
15. **写死 12 步/epoch、120 步、30 个 ckpt** → 余数 micro-batch 的处理随 transformers 版本变，以 `trainer_state.json` 为准
16. **`paths.output_dir` 写成 `results/...`** → cwd 是 `open-unlearning/`，会落进库目录；一律 `../results/...`
17. **只做 `tofu_full` 一份评测配置** → "精简评测"没有命令，agent 每次跑全套，操作点那一步就要 30 GPU 小时

**方法类**

18. **没对齐操作点就比较 $D$、$C$** → 比的是"跑了多远"，结论无效
19. **$D$、$C$ 不归一化** → 测的是改动幅度不是定位精度
20. **Fisher 用 `batch_size > 1`** → 算出来的不是 Fisher
21. **Fisher 在遗忘之后才算** → 顺序反了，数值没有意义
22. **RMU 阳性对照排在扫层之后** → gate 排在被 gate 的东西后面，等于没 gate
23. **拿 8 次 restoration 的离散度当显著性下限** → 那 8 次跑的是同一个模型，预期离散度为 0；显著性要用 bootstrap CI
24. **`mia_reference` 用 retain90 当参考** → 和 `forget_quality` 共用锚点，这个 MIA 就不独立了
25. **Layer Restoration 没有 $\Delta\theta$ 幅度对照** → 结论平凡
26. **Relearning 在全部 forget 数据上微调再在同样数据上测** → 测的是重新记忆，不是知识残留
27. **在 GA 上做 relearning 对照** → 不回弹可能是模型坏了，这一轴上是混淆的
28. **Gate 2 只有"零处为零"三条** → 什么都检测不到的 pipeline 也能过，要加"非零集里至少 1 层显著"
29. **S7 先删了 GD 的粗评 ckpt** → S10 的梯度冲突没得算；GD 的 keep 要留它们
30. **查 `requires_grad` 来确认 RMU 改了哪些参数** → `RMU.create_optimizer()` 最后一行 `_freeze_all_params(model, True)` 把所有参数设回 `requires_grad=True`，训练开始后打印一定是全部参数，这个检查永远不可能通过。要验只能靠 regex 预检 + `checkpoint-4` 的 `state_dict` 实锤
31. **扫层 / 移植 / relearning 共用一个 `paths.output_dir`** → 库在 `overwrite=False` 时按目录缓存已算过的指标，换了权重指向同一目录会静默返回上一个变体的结果，不报错。这是最容易产生"看起来跑完了、数字全是错的"的一个点
32. **regex 走命令行 override** → `(`、`|`、`\.` 要过 shell 和 Hydra 两层转义；写进 `scripts/configs/trainer/RMU_downproj.yaml` 用单引号，一层都不用
33. **只跑 1B** → 师兄明确要跨模型一致的规律
34. **用 LoRA 跑 3B** → LoRA 只改低秩旁路，$\Delta\theta$ 的分布模式和全参数完全不同，结论推不过去

**资源**

- **显存**：1B 全参数微调约 14–16 GB，24G 卡够；3B 约 40–42 GB，24G 卡跑不了。开 `gradient_checkpointing` 能省一截激活值，代价是慢 20–30%。
- **CPU 内存**：Fisher 字典和参数一样大。3B 的 $I_f$ + $I_r$ 要 24 GB CPU 内存，别只看显存。
- **磁盘**：`save_steps=4` 下单方法 30–32 个 ckpt 约 80 GB。逐方法串行，选完操作点立刻清理，峰值就压在 80 GB。清理一律先出清单；默认待人确认，`constants.yaml` 里 `resources.auto_delete_after_manifest: true` 可预授权自动删，但操作点 A/B、最终权重、GD 的粗评 ckpt 永不进删除列表。

---

## 报告里要主动交代的局限

先说比被问出来好：

1. **单 seed**。默认 `seed: 0`，六个方法各跑一次。方法间的小差异可能是噪声，只讨论跨族的大趋势，不解读同族内的细微差别。
2. **超参未调**。所有方法都用库的默认（lr 1e-5、10 epoch），操作点对齐部分缓解了这个问题，但不等于每个方法都在自己最好的状态。
3. **empirical Fisher**，不是 true Fisher；二阶近似只在 $\Delta\theta$ 小时可靠，对 GA 这类崩溃的方法最不可信——阶段 5 的验证一正好能量化这一点。
4. **DPO 与 NPO/SimNPO 数据设定不同**（idk vs 原答案），同族比较时不完全对等。
5. **TOFU 的 forget/retain 只是微调数据**，预训练语料动不了，所以"删干净"在这里是相对 retain model 而言的。
6. **relearning 只覆盖 2 个方法**（如果走的是最小版）。验证二是两端对照，不是趋势拟合；其余方法的遗忘不足证据来自 ES/MIA 和 Layer Restoration，这两者测不到"攻击者再微调"这个维度。
7. **单卡训练，与 repro.md 的 2×L40s + ZeRO-3 设置不同**。有效超参一致，但库作者明确说过分布式设置会影响结果，所以本工作的绝对数值不与 repro.md 对齐，只在内部横向比较。
8. **操作点判据是预注册的**（bootstrap 95% 上界 + utility 80% 底线），在看到结果之前定死。这是刻意的：事后挑操作点会让所有跨方法比较失去意义。80%/90% 两档一并报告。
9. **RMU_downproj 若调过 lr**，它只是 pipeline 的阳性对照，不进跨方法比较；报告里注明所用 lr。

---
---

# 术语表

## 数据与模型

**unlearning（遗忘学习）** — 让训练好的模型「忘掉」指定的一批数据，同时保住其他能力。动机是隐私合规（被遗忘权）、版权、有害内容清除。

**forget set** — 要被忘掉的那批数据。

**retain set** — 要保住的那批数据，是 forget set 的补集。

**holdout set** — 既不在 forget 也不在 retain 里的第三份数据，模型从没见过。MIA 指标靠它做对照：如果模型对 forget 数据的行为和对 holdout 的行为一样，说明真忘了。

**TOFU** — 一个遗忘学习基准数据集。200 个**虚构**作者 × 每人 20 条问答 = 4000 条。用虚构人物是为了确保这些知识只来自微调、不来自预训练，这样才有干净的对照。

**forget10 / retain90 / holdout10** — TOFU 的划分。forget10 是要遗忘的 10%（400 条），retain90 是剩下的 90%（3600 条）。两者相加是 TOFU 的全部，但**不是模型训练数据的全部**——模型还有万亿级的预训练语料你碰不到。

**target model** — 在 forget + retain 全集上微调过的模型，也就是「什么都记得」的起点。

**retain model** — 只在 retain 上微调、从没见过 forget 数据的模型。它是**黄金标准**：理想的遗忘结果应该和它一样。TOFU 的 `forget_quality` 就是拿遗忘后模型和它做统计检验。

---

## 遗忘方法

**GA（Gradient Ascent，梯度上升）** — 最朴素的做法：在 forget 数据上做梯度**上升**，让 loss 变大。问题是完全不管 retain，跑久了模型会崩。

**GD（GradDiff，Gradient Difference）** — GA 的扩展版，**同一步里同时**做两件事：forget 上升 + retain 下降。

$$L = -L_{\text{forget}} + L_{\text{retain}}$$

不是分阶段，是两个梯度相加后一次更新。

**NPO（Negative Preference Optimization）** — 借用 DPO 的形式，把「忘掉」表述成「相对于参考模型，降低对 forget 样本的偏好」，而不是无限拉高 loss。因此比 GA 稳得多。

**SimNPO** — NPO 的简化版本。注意它不在库的官方 baseline 脚本里，`docs/repro.md` 没有它的参考数值。

**DPO（Direct Preference Optimization）** — 原本是对齐用的方法，也可以改造来做遗忘。在这个库里 DPO 走 idk 数据：把 forget 问题的答案换成「我不知道」类回复，所以机制上更接近**输出替换**，和 NPO 的「偏好压低」不完全同类。

**RMU（Representation Misdirection Unlearning）** — 直接在某一层扰乱 activation，机制和上面几个都不同，属于 representation engineering。库里默认锚在 `model.layers.7`，但参数更新是全参数的。**这个「已知压制层」的性质使它成为阶段 3 的天然阳性对照。**

**方法族** — 同一族的方法机制相近，表现应当接近。师兄要求按族看趋势，而不是逐个孤立看。

**操作点（operating point）** — 拿来做跨方法比较的那个统一状态。因为不同方法在同样的 lr 和 epoch 数下会停在遗忘-效用权衡曲线的完全不同位置，不先对齐操作点，任何跨方法的参数级比较都在比「走了多远」而不是「走得准不准」。

---

## 评测指标

**forget_quality** — TOFU 的招牌指标。把遗忘后模型和 retain model 的 truth ratio 分布做 KS 检验，越接近说明越像「从没学过」。有黄金标准做锚点，这是它比 NLL 强的地方。

**model_utility** — 模型整体能力有没有坏。由 retain、real_authors、world_facts 三个分项聚合（每个分项各有 probability、ROUGE、truth ratio 三个数，共九个取调和平均）。

**extraction_strength（ES）** — 给一段前缀，模型能不能续出剩下的内容。测的是**实际生成**，不是 teacher forcing 下的概率。

**exact_memorization（EM）** — 逐 token 精确记住了多少。

**MIA（Membership Inference Attack，成员推断攻击）** — 判断某条数据有没有参与过训练。如果攻击成功，说明知识还在。open-unlearning 集成了六种：LOSS、ZLib、Reference、GradNorm、MinK、MinK++。

**MinK++** — 最强的 MIA 变体之一。你用 NLL 报「忘掉了」，别人用 MinK++ 一测可能就把知识捞回来了。

**NLL（Negative Log-Likelihood，负对数似然）** — 就是 loss 本身。**为什么它作为遗忘指标不合格**：① 无上界，GA 定义上就是把它推高，多跑几步能到任意大；② 没有参照点，说不了「多高算忘干净」；③ 它就是 MIA 里最弱的 LOSS attack；④ 分不清「知识删了」和「模型崩了」。

---

## 分析工具

**Fisher information（费雪信息）** — 衡量**参数重要性**的一种算法。参数重要性是概念（「这个参数动一下，模型表现掉不掉」），Fisher 是算它的具体方法：

$$I_i = \mathbb{E}\left[\left(\frac{\partial L}{\partial \theta_i}\right)^2\right]$$

**per-sample** 梯度平方在数据集上求平均——注意是逐样本，不是 batch 平均后再平方。同一个公式，喂 forget 数据得到 $I_f$，喂 retain 数据得到 $I_r$。

出处是持续学习里的 **EWC**（Kirkpatrick et al., 2017），当年就是用它来防灾难性遗忘的。

**empirical Fisher vs true Fisher** — 用数据集里的真实标签算梯度得到的是 empirical Fisher；true Fisher 要从模型自己的预测分布里采样标签。实践中都用前者，但它近似 Hessian 的条件更严（要求模型已经拟合得很好且在最优点附近）。**报告里要写清用的是哪个。**

**Hessian（黑塞矩阵）** — 所有二阶偏导排成的矩阵。一阶导（梯度）告诉你坡朝哪边斜，二阶导告诉你坡**弯得多厉害**。弯得厉害 = 参数动一点 loss 就飙 = 重要。

严格的参数重要性应该用 Hessian，但它是 $n \times n$ 的，1B 模型有 $10^{18}$ 个数，算不动。**Fisher 的价值在于：在最优点附近它近似等于 Hessian 的对角线，而只需要梯度平方。二阶的信息，一阶的代价。**

**influence function（影响函数）** — Fisher 的严格版，问「某条训练样本对模型的贡献有多大」。形式是 $-H^{-1}\nabla_\theta L(z)$，要算 Hessian 的逆，LLM 上做不动。写论文时可以作为理论出处引用。

**Δθ** — 遗忘前后的参数变化量，两个 `state_dict` 相减。

**随机方向对照（null baseline）** — 造一个和真实 $\Delta\theta$ 每模块范数都相同、但方向随机的扰动，用它算出 $D_{\text{rand}}$、$C_{\text{rand}}$ 作分母。**没有这个分母，$D$ 和 $C$ 测的是改动幅度，不是定位精度。**

**gradient conflict（梯度冲突）** — forget 梯度和 retain 梯度的夹角。用余弦相似度量化：

$$\cos\theta = \frac{g_f \cdot g_r}{\|g_f\|\,\|g_r\|}$$

负值 = 两个目标直接对抗，「忘掉 A」和「保住 B」要求参数往相反方向调。**注意**：GA 也有冲突，只是它不算也不管——冲突是数据和模型本身的性质，不是算法造出来的。

局限：它只能诊断灾难性遗忘。一个什么都不做的方法冲突为零，模型完好，知识一点没少——这个指标会给它满分。这也是师兄否掉它作为主方向的原因。

**Layer Restoration** — 把某层参数换回原始值，看知识回不回来。**干预**，不是测量，所以能给因果结论。测的是**必要性**。

**层移植（transplant）** — 反方向：把遗忘后模型的某层放进原始模型，看遗忘效果转不转移。测的是**充分性**。必要 + 充分都成立，才是干净的压制点。

**阳性对照（positive control）** — 用一个答案已知的案例来验证你的方法本身管不管用。这里 RMU 就是：它的压制层写在配置里，如果扫不出来，说明整条 pipeline 有问题。

**Activation Patching** — 更精细的干预：模型运行时，在某层某个 token 位置，把遗忘后模型的中间结果替换成原模型的。分辨率是「第几层 × 第几个词」，产出一张热力图。

因果力度最强，但工程量最大（要挂 forward hook、跑 $L \times T$ 次前向）。**陷阱**：标准做法是同模型不同输入，你这里是跨模型，如果两个模型表征漂移太远，搬过去的激活值就是分布外噪声。GA 跑崩的模型尤其危险。两周内不建议做。

**relearning（再学习）** — 遗忘后用少量数据再微调，看知识多快回来。快速恢复 = 知识只是被压制，没被删除。**要在微调数据之外的 forget 样本上评测**，否则测的是重新记忆。

它和 ES/MIA、Layer Restoration 的分工：后两者都在权重固定（ES/MIA）或由你手动改权重（Restoration）的前提下取证，只有 relearning 让**新的梯度更新**参与进来，对应「攻击者拿到模型后还能再微调」这个威胁模型。这是它不可替代的地方，也是它值得占一个排期位的唯一理由。

**CKA / PCA / SVCCA / Tuned Lens / Probe** — 都是 representation（隐层表征）分析手法，输出通常是「横轴层号」的图。师兄已经做过这块，注意避让。

---

## 两个失败模式对照

| | 灾难性遗忘 | 遗忘不足 |
|---|---|---|
| 现象 | 模型能力崩塌、输出乱码 | 看起来忘了，实际知识还在 |
| 指标 | `model_utility` 掉 | `extraction_strength`、MIA 仍然高 |
| 参数侧证据 | $D/D_{\text{rand}}$ 大（改到 retain 重要参数） | $C/C_{\text{rand}}$ 小（该改的没改） |
| 行为侧证据 | retain 微调修不回来 | forget 微调几步就弹回来 |
| 干预证据 | 换回受损层能修复 utility | 换回某层知识就回来 |
| 主要工具 | Fisher（有二阶展开撑着） | relearning、Restoration |

**Fisher 强在左边，relearning 强在右边，两个互补。** 单靠一个都有说不圆的地方——这也是师兄批评梯度冲突「只适合灾难性遗忘」的原因。而 Layer Restoration 是唯一两边都能给因果证据的工具，这是把它排在第一优先级的理由。

---

## 参考

- **OpenUnlearning 技术报告** — arXiv 2506.12618。框架设计、指标 meta-evaluation（faithfulness / robustness）、relearning 与 quantization stress test 的官方设定。
- **DA 类遗忘方法的失败分析** — arXiv 2509.26427。forget/retain 之间的数据依赖导致 ascent 类方法系统性失败；关于停止准则缺失和超参敏感性的论证，是阶段 1「操作点对齐」的直接依据。
- **EWC** — Kirkpatrick et al., 2017。Fisher 作为参数重要性度量的出处。
- **TOFU** — Maini et al., 2024。基准本身。
