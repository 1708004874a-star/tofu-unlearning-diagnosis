# 执行清单 v4 — 遗忘失败分析

**这份文件给执行者（人或 agent）看。** 设计理由、术语表、局限清单仍在 `实验流程_遗忘失败分析_v3.1.md`，那份是给人和汇报用的，执行时不必读。

**它不是操作手册。** 它只写三件事：① 库里那些你查不到、猜错就烧一天的坑；② 判据和阈值（这部分由人定死，任何情况下不许改）；③ 执行顺序。**目录结构、文件格式、脚本怎么实现，你自己定。**

---

## v4 相对 v3.1 的取舍

| 砍掉 | 理由 |
|---|---|
| 梯度冲突（原 S10） | 只对 GD 有定义；只诊断灾难性遗忘（不作为的方法得满分）；师兄已否掉作主方向，他自己排第 4 位 |
| Fisher 的 $D$ / $C$ / 随机方向对照 / 验证一（原 S9、S17 验证一） | 这正是师兄两篇论文的核心工具，重做拿不到差异化。Fisher Overlap 保留为增量段 E |
| relearning 全套 + 验证二（原 S16、S17 验证二） | 同上（师兄已用过 relearning）。降级为段 F 的单条曲线 |
| SimNPO | 无 repro 参考值，且是第三个 preference 方法，故事价值最低 |
| Activation Patching | 跨模型搬激活在跑崩的模型上是分布外的；作为 next step 写进报告 |
| 产物 JSON schema、`status.py`、manifest 人工确认流程、11 条 STOP-ASK | 过度规范。降为"产物存在即可" + 4 条停机条件 |

| 改掉 | 理由 |
|---|---|
| **Gate 2 第 4 条从"停机"改成"分支"** | §0.4 的两个精确等式测试已从机械上证明管道可用，此时第 4 条失败只能是"单层探针不够灵敏"（知识分布式），那是结论不是故障。见 §4 段 C |
| `save_steps` 4 → **10** | 一个 ckpt 2.5 G 是死的（1.24B × bf16）。`save_steps=4` 是 30 个 = 75 G，把 85 G 卷吃光；`save_steps=10` 整除 120，落 12 个 ckpt = 30 G，加最终权重 32.5 G/方法，峰值约 56.5/85（见 §6）。代价是操作点分辨率从 4 步变 10 步：选中的 A **更深入遗忘**，恢复量绝对值跟着偏大 → **每张恢复量的图都要标注该方法的 overshoot**，让读图的人自己判断可比性。<br>**[2026-09-04 平台迁移]** 原文按 AutoDL 50 G 单盘定为 12（10 个 ckpt）；换到 RunPod 85 G 网络卷后收紧到 10，分辨率更高、overshoot 更小。脚本 `run_pipeline.py:SAVE_STEPS` 是 10，以脚本为准 |
| **取消粗筛/细评两段式，全部 ckpt 全评** | 两段式当初只为省 GPU：30 个 ckpt 时只评 9 个。现在只剩 12 个，**全评和「粗筛 8 + 细评 1」成本几乎一样**（单指标约 2 分钟/次），却要多写「找越线区间 / 首点已越线 / 从未越线」三段可能出错的逻辑。砍掉是净赚。代价见 §3 最后一条 |
| 操作点轨迹只跑 `forget_Q_A_ROUGE` | `model_utility` 要在 600 条上生成，定轨迹时用不到。省每方法约 1 小时，不影响任何数值 |
| 执行顺序改成"前缀式" | 段 A 跑完即是一个完整可交付的 pilot，之后每段都是独立可加、独立可讲的增量 |

**没动的**：lr 1e-5、10 epoch、seed 0、有效 batch 32、全 400 题评测、flash-attn。见 §1。

---

## 0. 分工

**你负责**：`git clone`、装环境、读源码、写脚本、跑、把中间量给我看。

**我负责**：判据和阈值（§3）。**这部分不许你改，也不许你"看完结果再定"**——事后挑点会让所有跨方法比较失去意义。

**你必须回报的**：§2 里每一条断言，读完源码后给我 `文件:行号` 的确认或推翻。那些说法是二手的，我需要你验证过才敢写进报告。

---

## 1. 不可协商的约束（保真优先）

这个实验的可信度建立在"我们的绝对数值能和官方日志对上"。任何改变训练/推理数值的偏离都要先问我。

1. **flash-attn 2.6.3 必须装。** 注意这不是「换个后端」的问题：`configs/model/Llama-3.2-1B-Instruct.yaml` 里 **`attn_implementation: 'flash_attention_2'` 是写死的**（已查证）。不装 flash-attn 就必须去覆写这个字段——那是实打实的数值偏离，而官方 `TOFU_EVAL.json` 正是带着它跑出来的。覆写之后 Gate 1a 就多了一个你说不清大小的偏差来源，那个 gate 废掉一半。
   - **不要源码编译**。去 flash-attention 的 GitHub releases 挑预编译 wheel，30 秒装完。
   - 文件名形如 `flash_attn-2.6.3+cu123torch2.3cxx11abiFALSE-cp311-cp311-linux_x86_64.whl`，四项必须对上：CUDA 大版本 / torch 版本 / Python 版本 / cxx11abi 标志。
   - 查环境：`python -c "import torch; print(torch.__version__, torch.version.cuda, torch._C._GLIBCXX_USE_CXX11_ABI)"`，最后那个 True/False 要和文件名里的 `cxx11abiTRUE/FALSE` 一致，对不上装了也 import 不了。
   - **顺序是先看 wheel 再挑镜像**，不是反过来。2.6.3 覆盖的大致是 torch 2.2–2.4 那一带。目标环境：**Python 3.11 + torch 2.3/2.4 + cu121/cu123**。
   - 实在只能编译：`MAX_JOBS=4 pip install flash-attn==2.6.3 --no-build-isolation`（不限流会把内存吃光被 OOM-kill）。
2. **torch 跟 `requirements.txt` 走 `2.4.1`，不是镜像自带的 2.3.0**【2026-09-03 改判】。`requirements.txt:7` 钉 `torch==2.4.1`、`numpy==2.2.3`（2.3.0 不支持 numpy 2），`pip install -e .` 本来就会把镜像的 2.3.0 换掉。§1 的口径是对齐官方，**requirements.txt 就是官方口径，硬压回 2.3.0 才是偏离**。flash-attn wheel 随之改成 `+cu123torch2.4cxx11abiFALSE-cp311`。
3. **模型配置里的 `attn_implementation` 和 `torch_dtype` 照抄，不要覆写。** 已查证 `configs/model/Llama-3.2-1B-Instruct.yaml` 是 `flash_attention_2` + `bfloat16`（bf16 也是 ckpt ≈ 2.5 GB、§6 磁盘估算成立的前提）。开工时 `grep -rn "attn_implementation\|torch_dtype" open-unlearning/configs/model/` 跑一次，**只为确认 commit 没漂**，不是去发现它写的是什么。
3. **评测用全部 400 条，不许抽子集。** 子集会破坏与官方日志的可比性，省下的时间不值这个价。
4. **有效 batch 必须 = 32。** 优先 `per_device=8 × accum=4`（repro.md 的口径）；第一天在 NPO 那一跑上量显存峰值，24G 装不下再退到 `4 × 8` 并告诉我。
5. **`gradient_checkpointing` 保持 `true`，与官方一致 —— 但库默认是 `False`（`configs/trainer/finetune.yaml:14`），必须每次显式传，漏了就是偏离。**`scripts/tofu_unlearn.sh` 第 63 行是脚本在覆写成 true，官方 baseline 带着它跑，**repro.md 的数字里已经含了重算激活的那点浮点差**——所以关掉它才是偏离。它慢 20–30%，但只落在训练上，而训练总共约 2 小时，代价是半小时，不值得为此偏离。副作用是省显存，`per_device=8` 更容易装得下。
6. **lr 1e-5、10 epoch、seed 0 一律用库默认，不许调。** 唯一例外见 §4 段 D 的 RMU_downproj。
7. **不改 `open-unlearning/` 下任何文件。** 配置定制走 `--config-dir` 指向自建目录。真改不可，把 patch 存下来并告诉我。
8. **10 个 epoch 不许砍。** 很诱人想砍到 5，但 NPO 可能到第 10 个 epoch 才刚开始遗忘，砍了就可能永远越不过阈值、操作点为空，全盘皆输。

---

## 2. 库里的坑（你查不到，逐条源码确认后回报）

这些是这份文件最有价值的部分。前 4 条是会产生"看起来跑完了、数字全是错的"那一类。

1. **评测在 `overwrite=False`（默认）时按 `output_dir` 缓存，且只认目录不认模型。**`src/evals/base.py` 的 `evaluate()` 先读目录下已有的 `TOFU_EVAL.json`，逐指标 `skip already evaluated`。换了权重指向同一目录，它会把上一个变体的结果当成已算过返回，**不报错不警告**。
   → **扫层 / 移植 / relearning 的每一个变体必须各用自己的 `output_dir`。** 这是全流程最容易出错且最难发现的一点。
   → **同一条规则的另一面：这就是你的断点续跑。** 只要目录不复用，中断后原样重跑会自动跳过已算完的指标（粒度细到单个 metric），不必从头来。
   → **但 flush 发生在每个顶层指标算完之后**（`src/evals/base.py:109-110`）：`model_utility` 内部那九个子指标是**一个整体**，中途断掉整个重来（约 45 分钟）。`cheap` 的两个指标没这个问题。v4 没有 `.done` 哨兵也没有状态文件，**这是唯一的恢复机制**——段 A 有 20 小时，会话必然断一次，说清楚了你才敢直接重跑。
2. **`RMU.create_optimizer()` 最后一行是 `_freeze_all_params(model, True)`**，optimizer 构造完之后所有参数的 `requires_grad` 又被设回 True。所以**查 `requires_grad` 来确认 RMU 改了哪些参数，这个检查永远不可能通过**。要验只能靠 regex 预检 + 比对 `state_dict`。
3. **eval 漏传 `model.model_args.pretrained_model_name_or_path`** → 静默评测默认 target model，不报错，数字看起来还挺正常。
4. **eval 漏传 `retain_logs_path`** → `forget_quality` 算不出来（返回 `None`，至少你看得见）；**`holdout_split` 和 `forget_split` 不匹配** → MIA 类指标全废。
    **真正危险的是 `privleak`**【2026-09-03 新增】：`src/evals/metrics/privacy.py:43-49` 在没有 retain 日志时不返回 `None`，而是 `except` 掉、回落到 `configs/eval/tofu_metrics/privleak.yaml:17` 的 `ref_value: 0.5`，**照常算出一个看起来完全正常的百分数**。`privleak` 是"遗忘不足"轴的指标之一，这个假数会直接进报告。
5. **`save_strategy` 默认是 `'no'`** → 训练全程一个中间 ckpt 都不存。必须显式设 `steps`。
6. **`setup_data.py --idk` 是独立的一项**，README 的 Quickstart 没写。漏了 DPO 找不到 `data/idk.jsonl` 直接崩。（`--eval` 会被 argparse 前缀匹配到 `--eval_logs`，但 `--idk` 不会。）
7. **`configs/experiment/unlearn/tofu/idk.yaml` 里 `override /model` 写的是 3B**，跑 DPO 必须显式传 `model=Llama-3.2-1B-Instruct`。
8. **自建评测配置不得叫 `tofu.yaml`**——【2026-09-03 实测修正，机制与原文相反】不是你 shadow 掉库，而是**库赢、你的文件被静默忽略**。Hydra 1.3 的 searchpath 顺序是 `hydra → main（库 configs/）→ command-line（--config-dir）→ schema`，第一个命中就赢。后果比"看不出加载哪份"更糟：你以为在跑自己的 `cheap`，实际跑的是库默认全套，**慢 20 倍且指标不对，一句 warning 都没有**。用 `cheap_v4` / `full_v4` 这类名字。（同一组实测的好消息：`eval=cheap_v4` **能**压过 experiment 里的 `override /eval: tofu`。）
9. **工作目录是 `open-unlearning/`**（`src/train.py`、`saves/` 都相对它）。自写产物的 `paths.output_dir` 一律写 `../results/...`；写成 `results/...` 会落进库目录里。
10. **有效 batch**：库默认 `per_device=8 × accum=4` 单卡就是 32；`tofu_unlearn.sh` 写的 `4 × 4` 是给 2 卡用的，抄到单卡有效 batch 就变成 16，对不上 repro.md。
11. **RMU_downproj 的 regex 里不要给 `|` 加反斜杠。**`\|` 在 Python `re` 里是字面竖线，模式匹配不到任何参数，训练等于没动，最后会被误判成"到不了操作点"。而且 regex 走 yaml 单引号，**不要走命令行 override**（`(`、`|`、`\.` 要过 shell 和 Hydra 两层转义）。
12. **不要照 `src/trainer/base.py` 顶上那句 "Modified from v4.45.1" 把 transformers 降版。**`requirements.txt` 钉的是 `4.51.3`，那句注释只是说明代码抄自哪里。
13. **RMU 的两项 loss（forget 的激活-control vector MSE、retain 的 `EMBED_DIFF`）都建在 `model.layers.7` 的输出激活上，loss 的计算图止于第 7 层。**所以 8–15 层的 `.grad` 是 `None`，AdamW 整个跳过它们，`weight_decay` 也不作用（PyTorch 只对 grad 非 None 的参数施加 decay）→ **这些层的 Δθ 是精确的 0**。这是段 C 整个 gate 的理论依据。
    【2026-09-03 实测确认，两处补充】① 优化器不是 torch AdamW，是 **`paged_adamw_32bit`**（`configs/trainer/finetune.yaml:15`，`docs/repro.md:26` 也写明）。跳过逻辑相同：`bitsandbytes/optim/optimizer.py:284` 的 `Optimizer8bit.step()` 里同样是 `if p.grad is None: continue`，`weight_decay: 0.01` 也够不着。**Δθ 精确为 0 成立。** ② **`model.norm`（第 15 层之后的 final RMSNorm）同样在计算图下游，Δθ 也应为 0**，`gate_2.RMU.layers_must_be_zero` 没列它——Gate 2 顺手一起查，不花钱。
14. **步数：`max_steps = 120`，不是 130**【2026-09-03 实测推翻原文】。`transformers 4.51.3` `trainer.py:5303` 用的是 **floor**：`num_update_steps_per_epoch = max(len_dataloader // grad_accum, 1) = 50 // 4 = 12`，`:5306` `max_steps = ceil(10 × 12) = 120`。
    **但底下藏着一件真事，方向和原文相反**：`trainer.py:2517` 的 `do_sync_step` 除了整除还认 `(step+1) == steps_in_epoch`，所以**每个 epoch 实际走 13 个优化器步**，120 步在第 10 个 epoch 中途就到了 → `trainer_state.json` 里的 `epoch` 停在 **≈9.24**，不是 10.0。
    三点后果：① `constants` 里 `total_steps=120 / steps_per_epoch=12 / expected_checkpoints=12` **本来就是对的**（2026-09-05 订正：此处曾误写 10，constants-v4.yaml 权威值一直是 12，5 个方法训练实测也都是 12 个 ckpt），回填只是走个确认；② `save_steps=10` → ckpt 落在 10,20,…,120，正好 12 个，**且 step 120 与训练结束重合**；③ **报告里不能写"训了 10 个 epoch"，实际是 9.24**——这不是我们的偏离，官方 repro 走同一段代码、同样 `max_steps=120`，但要如实写。
15. **禁止跨 run 混用 ckpt。**GPU 训练非逐位确定，重跑出来的不是同一条轨迹。同一方法的所有下游分析必须来自同一次 run。
16. **`eval_strategy` 设 `no`，`eval_on_start` 也要一起设 `false`**（`configs/trainer/finetune.yaml:21` 默认是 `True`，只关一个不够）。默认是 `epoch`，训练中会跑完整评测（很慢），而且它只在单进程下生效（`num_processes != 1` 时直接返回空，只 warning）。评测统一离线做。

---

## 3. 判据（我定死的，不许改）

所有数值在 `constants-v4.yaml`。这一节只说规则。

**Gate 1a（硬停）**：不训练，直接评 target model，与 `setup_data.py --eval_logs` 下载的同名日志比对。概率类 / truth ratio 类容差 `tolerance_prob`，ROUGE 类 `tolerance_rouge`（ROUGE 放宽是因为贪心解码跨硬件会翻 argmax）。偏差落在容差的 80%–100% 之间不自动放行，diff 逐样本生成后交我定性。

**Gate 1b（软）**：NPO 的**最终权重**用**库默认评测配置**对 `docs/repro.md`。`model_utility` 或 `forget_truth_ratio` 差超过 `hard_stop_delta` → 停；以内只记录、继续（2026-09-05 订正：此处曾误写 `forget_Q_A_ROUGE`，实际比的是 `model_utility`+`forget_truth_ratio`——repro.md 的 1B 表格没有 `forget_Q_A_ROUGE` 这一列，原判据够不着参照物，见 constants-v4.yaml `gate_1b.compare_metrics`）。**`forget_quality` 不设判据**（KS 检验 p 值能差几个数量级）。repro.md 是 2×L40s + ZeRO-3 跑的，且明确警告单卡结果会变，所以它不能当硬 gate。

**操作点阈值**：retain90 逐样本 `forget_Q_A_ROUGE`（`TOFU_EVAL.json` 里的 `value_by_index`）做**均值的 bootstrap，取 95% CI 上界**。全局算一次。不用点估计，是因为"首次降到"可能触发在一次抖动上。

- **操作点 A** = 首个 `forget_Q_A_ROUGE ≤ 阈值` 的已评测 ckpt。**「已评测」= 全部 12 个 ckpt + 最终权重**，全评，不做粗筛/细评两段式，也不许自己发明子集
- **没有操作点 B。** v3.1 有一个「效用侧操作点」（utility 仍达标的最后一个 ckpt），v4 砍掉了：它在 v4 里不被任何下游分析消费（全部锚在 A 上），而要**正确**定出它必须在每一个 ckpt 上都测 utility，每方法多花约 45 分钟。只在 A 附近的点上测再挑「最后一个达标的」，会把 B 系统性地拉到 A 附近——那是个错的 B，比没有更糟。
- **utility 只在三个点上测**：第一个 ckpt、A、最终权重。用途是 ① 判可行性（A 点）② 遗忘-效用权衡曲线的三点骨架 ③ GA 那类方法「utility 在越线之前就已经崩了」的证据。**A 不存在时改测首 / 中 / 末三点。**
- **可行性** = A 存在 **且** `model_utility(A) ≥ baseline × 0.80`。A 到最终权重仍未出现也算不可行。`feasible_check` 同法用 `utility_floor_check`（0.90），两档一并报告——这是 0.90 那一档**唯一**的用途
- **overshoot** = 阈值 − A 的 ROUGE，超过 `overshoot_flag` **只标记不重跑**（重跑是另一条轨迹，混用会出问题）。overshoot 大 = 这个方法在两个 ckpt 之间冲过头了，它的 A 比别人更深入遗忘，恢复量天然偏大 → **所有恢复量的图都必须把 overshoot 标在旁边**
- **一条失效的保护，只能承认**：原方案有个特殊处理——若第一个评测点就已越线（遗忘比第一次观察还早发生），就回头评起点与第一点之间的 ckpt 把 A 找准。`save_steps=10` 下第 10 步之前一个 ckpt 都没有，**没得评**，这类方法的 A 只能取 step 10、overshoot 很大。85 G 卷下也无解：真要覆盖第 10 步之前就得 `save_steps` 再降一档，30 个 ckpt = 75 G，卷装不下。最可能触发的是 GA，而 GA 大概率本就判不可行；若 NPO 或 DPO 也在 10 步内越线，那批比较要打折，报告里必须讲
- 不可行的方法退出跨方法比较，但仍在图上用空心点画出——它正是"失败"的一个干净定义
  > **【2026-09-05 澄清】** "退出跨方法比较"指退出统计聚合/跨方法比较，**不等于退出层级扫描**。
  > 本次 NPO/RMU/DPO/GA/GD 五个方法跑下来，除 RMU 外全部判定不可行——若把"不可行就不扫层"
  > 当通用规则套用到 §4 段 A 第 9 步，只剩阳性对照 RMU 能扫，主分析没有对象。
  > 判据、阈值、feasible/feasible_check 的判定方法本身**不动**，只改"不可行的方法进不进扫层"
  > 这一条分支：NPO、DPO 无论 feasible 与否都照第 9 步扫层，问题从"换回哪层能拿回被遗忘的
  > 答案"改述成**"哪层承载了这次 utility 损伤"**（对应第 11 步图②"换回哪层能修回
  > model_utility"）——仍是层级的、因果的分析，只是问题换了个方向。
  > **【2026-09-05 机制落地】** model_utility 的 agg_value 是 9 个子指标的调和平均（含 KS
  > 检验 p 值），实测确认没有 `value_by_index`，套不进 §3"逐样本配对 bootstrap"那套显著性
  > 检验。改用 **`retain_Q_A_ROUGE`** 承担 catastrophic 轴的显著性结论——结构上跟
  > `forget_Q_A_ROUGE` 完全一样（400 条 `value_by_index`，`agg_value` = `rougeL_recall` 均值，
  > 已实测确认），且本来就在 `metrics.axis_mapping.catastrophic` 里，直接复用同一套 bootstrap
  > 机制。`model_utility` 本身仍然只画不判（跟 `forget_quality`/`privleak` 同一待遇）。
  > 阳性对照 RMU 的 16 层 sweep 也要补跑这一条，保证段 A 三个核心分析对象在 catastrophic 轴上
  > 有同一套可比数据（见 `constants-v4.yaml: restoration.significance`）。段 B 里 GA/GD 那条
  > "不可行就不扫层、按泄压阀删 A"维持原样：它们的扫层不是取消，是**推迟到段 B**，泄压阀
  > 那条删除规则够不着"只是排到后面"的情况，GA/GD 的 A 点本次保留。

**恢复量显著性**：逐样本**配对**恢复量（同一题：换层后 − 遗忘后）做 bootstrap，CI 下界 > 0 才算显著。每方法 16 次检验，CI 水平按 Bonferroni 取 `1 − 0.05/16`。**不许**拿"同一模型评多遍的离散度"当显著性下限——那个预期就是 0。

**Gate 2（阳性对照）**：见 §4 段 C。

---

## 4. 执行顺序（前缀式：每段停下来都是完整的）

### 段 A — 主线（约 20 小时，跑完即是一个可交付的 pilot）

分析对象只有 **NPO / RMU / DPO-idk** 三个方法，但**训练一次训 5 个**（GPU 只要 2 小时，先训完省得以后补）。

1. **环境**（§1）+ **磁盘布局**（`disk_layout`）。**平台是 RunPod**（2026-09-04 从 AutoDL 迁移）：network volume 挂在 `/workspace`，**85 G 配额、跨停机保留**；container disk **停止即清空**，不能放任何需要活过停机的东西。
   - conda（`/workspace/miniconda3`）、环境 `/workspace/envs/unlearning`（python 3.11）、repo `/workspace/open-unlearning`、HF 缓存 `/workspace/.cache/huggingface`、结果 `/workspace/results` —— **全部放 volume**，固定开销共约 14 G
   - **`saves/` 不需要软链**：repo 本身就在 volume 上，库写到 `open-unlearning/saves/` 自然落在 volume。（AutoDL 时代那条 `ln -s /root/autodl-tmp/saves` 已作废）
   - **只有 `/root/tmp_restore`（扫层/移植的临时权重，用完即删）故意留在 container disk**，跟 ckpt 的峰值完全错开
   - **开工先 `mountpoint -q /workspace` 确认卷真的挂上了**，没挂上的话下面所有东西都会写进 container disk、一停机全没（`remote_setup.sh:45`）
   - **判断有没有超预算只能看 `du -s /workspace` 的实占，不能看 `df` 的 Avail**：volume 是共享 MooseFS，`df` 报的是整个机房集群的剩余量（几百 T），永远不会告警（`remote_setup.sh:46-47` 实测）
   - **`pip install --no-cache-dir`**（或装完 `pip cache purge`）：装 torch 之后 `~/.cache/pip` 能到 3–5 G
   - 装完立刻 `pip freeze` 存档
2. **数据**：`setup_data.py --eval_logs` 和 `setup_data.py --idk` 都要跑。确认 retain90 的日志里 `forget_Q_A_ROUGE` 带 `value_by_index`（操作点阈值要用）；没有就用默认配置自评一次 retain90。
3. **Gate 1a** → 不过就停。**下载的日志里没有 target(full) 那份时**，按 `gate_1a.fallback_reference` 改对 `docs/repro.md` 的 target 行（容差同上），并在快照里注明用的是 fallback。通过后记下 target 自己的 `model_utility`，那是可行性判定的 utility 基线。
4. **两份自建评测配置**（不叫 `tofu.yaml`）：
   - `cheap`：`forget_Q_A_ROUGE` + `extraction_strength` —— 扫层、移植、等式测试用
   - `full`：库默认全部 + 取消注释 `exact_memorization` 和 `mia_min_k_plus_plus`；**`mia_reference` 保持注释**（参考模型若用 retain90 会和 `forget_quality` 共用锚点，这个 MIA 就不独立了）
   - 两份都 dry run，确认 `eval=<name>` 能压过 experiment 里的 `override /eval: tofu`，且指标集正确。各记一次实测耗时。
   - 顺手用 `cheap` 评一次 target 存下来（归一化和等式测试都要它）。
5. **训练 5 个 run**：NPO、RMU、DPO-idk、GA、GD。`save_strategy=steps save_steps=10 eval_strategy=no eval_on_start=false do_eval=false`。**NPO 先跑**，完了立刻做 **Gate 1b** 并从 `trainer_state.json` 回填真实步数（§2.14）。DPO 走 `experiment=unlearn/tofu/idk` 且显式传 1B 模型（§2.7）。

   > **严格串行仍然是硬规则**（`training.strict_serial`），85 G 卷没有放宽它：
   > **训一个 → 走第 6 步选出操作点 A → 清理 → 再训下一个。**
   > 5 个方法连着训完再统一处理 = 5 × 32.5 GB = **162.5 GB，必爆盘**。串行 + 清理的峰值恒为 32.5 GB + 已保留的 A。
6. **操作点**（每个方法训完立刻做，别攒着）：
   - **全部 12 个 ckpt + 最终权重全评**，只跑 `forget_Q_A_ROUGE`（单次约 2 分钟，一个方法约 26 分钟）。不做两段式
   - 补 utility：**只在三个点上跑**（第一个 ckpt、A、最终权重），定 `feasible` / `feasible_check`
   - 对 **A** 跑一次 `full` 留档 —— 遗忘不足轴的 ES / EM / MinK++ / privleak 全从这里取。**只有 A 需要留档**
   - **然后立刻清理**（`resources.cleanup`）：删掉该方法除 A 以外的全部 ckpt。一个方法训完有 25 GB 快照，你只需要 A 那一个，其余 9 个的使命是帮你找到 A，找到后就是死重，不删下一个方法没地方训
     - **例外**：NPO 的最终权重留到 Gate 1b 做完再删（Gate 1b 比的就是它，那是唯一的外部校验）
     - **清理脚本必须硬编码跳过 `saves/eval/`**：`setup_data.py --eval_logs` 下的官方日志也落在 `saves/eval/`（几十 M，不占地方），且管道自己所有评测产物（`opA_*` / `util_*` / `archiveA_*` / `gate1b_*`）都写在这里。误删了 Gate 1a 的参照物和操作点阈值的来源就全没了，而且要重新下载
     - **删之前，要留的 ckpt 先 `from_pretrained` 加载一遍**。删除不可逆，而 ckpt 可能写坏（写盘中断、盘满）；留下坏文件就要整跑重来，而重跑的轨迹与原来**不是同一条**，前面基于旧轨迹的分析全部作废。10 秒换几小时
     - **泄压阀（`cleanup.after_sweep`）——范围比想象的窄**：只有**判为不可行、因而根本不扫层的方法**（大概率是 GA），其 A 在操作点分析做完后即可删。
       **可行方法的 A 不能在扫层后就删**：段 E 要在 top-3 层上重做 restoration 跑 `full`、段 F 的 relearning 要从 A 起训，两处都还要用它。只有当你确定不做段 E/F 时，可行方法的 A 才能在扫层后删
7. **写 restore.py**（含 transplant 模式），**先过两个精确等式测试再放全量**：
   - 把 RMU 所有 Δθ≠0 的层**全部**换回 target → `cheap` 结果必须与 target 的 `cheap` 自评**逐位相同**（含 `value_by_index`）
   - 把所有 Δθ≠0 的层**全部**移植进 target → 必须与 RMU 遗忘后模型的 `cheap` 自评逐位相同
   - 自动判定，不需要我看中间量。任一不等 = 脚本有 bug，不许往下走。**跳过它们省 20 分钟，返工要 4 小时。**
8. **段 C 的 Gate 2**（RMU 那一半）—— 见下。**必须在扫其他方法之前**。
9. **扫层**：NPO 和 DPO 各 16 层，`cheap`，每层各用自己的 `output_dir`（§2.1）。同时记录每层的 `‖Δθ_layer‖`。
   **无论 NPO、DPO 是否判定 feasible 都要扫**——见 §3 判据末尾【2026-09-05 澄清】，不可行不等于跳过扫层。

   > **临时模型目录写在 container disk**（`/root/tmp_restore/`），**不要放 volume**——它用完即删、不需要活过停机，放在卷外能和 ckpt 的峰值完全错开。
   > **任一时刻最多存在一个**（`restoration.temp_model_policy`）——写一份 → eval → **立刻删** → 再写下一份。
   > 每一份换层后的模型都是完整的 2.5 G 拷贝。若先把 16 层的变体全写出来再统一评测，就是 **40 G，直接爆盘**。
   > 更省事的做法是进程内直接调库的评测器、根本不落盘。
10. **移植**：每方法恢复量最高的那 1 层，反方向做一次。
11. **出图**（4 张）：① 逐层恢复量 vs `‖Δθ_layer‖`（三个方法叠在一起）；② 换回哪层能修回 `model_utility`；③ 三个方法在操作点 A 的 ES / MinK++ 对比；④ 必要性 × 充分性的散点。
12. **写报告**（8 页）。

**段 A 的结论长这样**：三种不同机制的方法，压制点分别落在哪里、是不是同一批层、是不是只是输出压制。带阳性对照，所以可信。

### 段 B — 加跨族（+6 h）

GA、GD 的**扫层**（它们的训练和操作点已在段 A 第 5–6 步随其余方法一起做完了，段 B 只做扫层）。GA 大概率不可行（forget 降下来之前 utility 就崩了），那本身就是"灾难性遗忘"的干净证据，用空心点画上，不进任何跨方法统计；不可行就不扫层，其 A 按泄压阀规则可删。

拿到的东西：跨族趋势（无约束 / 带 retain 项 / preference / 表征工程），这是师兄明确要的。

### 段 C — Gate 2：阳性对照（RMU 部分在段 A 内，RMU_downproj 是 +4 h 的增量）

**为什么必须排在扫其他方法之前**：gate 排在被 gate 的东西后面等于没 gate。

**对照 ①：RMU**（段 A 第 8 步做）。理论依据是 §2.13。四条判据：

1. 8–15 层 Δθ **严格为 0**（逐元素比 `state_dict`，不用评测）
2. 这些层的 restoration 恢复量**严格为 0**（换回等于没换）
3. `significant` 的层**全部**落在 0–7 层内
4. 0–7 层里**至少 1 层** `significant`

> **第 4 条失败时不要停机，改走分支。**§4-A-7 的两个等式测试已经从机械上证明了 restore 管道是通的，所以此时第 4 条失败不可能是"pipeline 什么都没检测到"，只可能是**单层探针不够灵敏**——RMU 改了 0–7 全部 8 层加 embedding，压制很可能是冗余分布的。
> 分支做法：**累积换回**——先把 0–7 全部换回，若显著则二分（0–3 / 4–7），再二分。约多 4–6 次评测。
> **前 3 条任一失败才是真停机**，那说明 restore 代码有 bug 或评测噪声底太高。

**顺带的确定性检查**：8–15 层那 8 次 restoration 跑的其实是同一个模型。贪心解码 + 固定 batch，预期 8 次结果逐位相同、离散度为 0。非 0 说明评测本身不确定，影响所有后续比较，立刻告诉我。

**对照 ②：RMU_downproj**（增量段）。把 `trainable_params_regex` 覆写成只匹配 5–7 层的三个 `mlp.down_proj.weight`（RMU 原论文做法，库配置里注释着）。它比 RMU 更完整：13 层已知为零（特异性）+ 3 个已知非零的矩阵（灵敏度），两头都有已知答案。

- **走配置文件不走命令行**（§2.11）。`defaults: [RMU, _self_]`，`_self_` 必须在后，否则 RMU 的 `method_args` 会盖掉你的覆写。regex 用单引号包住。
- **两级校验，都不看 `requires_grad`**（§2.2）：训练前用同一个 regex 在 `named_parameters()` 上 `re.fullmatch` 计数必须 == 3；`checkpoint-8` 存出后逐张量比 `state_dict`，Δ≠0 的必须恰好 3 个。任一不符 → 停。
- 它到不了操作点 A 时，**允许只对这一跑按 lr ladder 调学习率重跑**（它不参与跨方法比较，调 lr 不影响结论）。ladder 用完仍到不了 → 记录后跳过，Gate 2 只剩 RMU 那一半，报告里注明。
  > **换下一级 lr 之前，先把上一跑的 ckpt 全删掉**（`resources.cleanup.lr_ladder`）。这一跑**没有 A**，所以是**全删**，一个不留——§4 段 A 第 6 步那条清理规则的触发条件是「定完 A 之后」，而这里恰恰是 A 不存在，**规则够不着这个分支**。ladder 最多 3 跑，不删就是 3 × 32.5 = **97.5 G，必爆**（加上 14 G 固定开销更没得商量）。

### 段 D — Fisher Overlap（+4 h）

只算重叠，**不要算 $D$、$C$、随机方向对照**（那部分砍了）。

- 在**原始 target model** 上算，不是遗忘后的模型（Fisher 近似 Hessian 只在最优点附近成立）
- **`batch_size` 必须是 1**：要的是 per-sample 梯度平方的期望；batch > 1 累加的是"batch 平均梯度的平方"，差约 $B$ 倍，样本间方差还被平均掉了，算出来的不是 Fisher
- **累加器常驻 GPU**（fp32，1B 约 5 GB × 2），全部跑完再一次性落 CPU。每步都往 CPU 搬 5 GB 的话，PCIe 上要走 4 TB，几小时起
- $I_f$ 用 forget10 全部 400 条；$I_r$ 从 retain90 随机抽 400 条保持对称
- **复用库的 dataset / collator**，chat template 和 answer-only 的 `-100` mask 必须和训练一致，不要自己 tokenize
- 各自归一化到和为 1 后取 $\sum_i \min(\hat I_f, \hat I_r)$，聚合到模块级
- 先在 20 条上跑通、打印几个参数的量级给我看，再放全量

**出的图**：横轴层号，Fisher Overlap 和 restoration 恢复量叠在一起。两条对得上 → "Fisher 能预测因果位置，可以当便宜的代理"；对不上 → "参数重要性 ≠ 因果压制点"。**两种结果都是结论**，这也是它值得占 4 小时的唯一理由。

### 段 E — 层内定位（+5 h）

对每个方法恢复量前 3 的层跑 `full` 全套指标；再把这几层拆成 attention / MLP 分别换回。模块细分只在 RMU 上做一次当示例即可。

### 段 F — relearning 单曲线（+3 h）

只做一个方法（挑扫层恢复量最大的那个），不做跨方法比较。

- **在 forget10 的 10% 子集（40 条）上微调，在剩余 360 条 held-out 上评测。** 官方设定是在全部 forget 数据上微调再在同样数据上测——那测的是"重新背下来"，不是知识残留
- 从该方法的操作点 A 起，`trainer=finetune`，lr 2e-5，有效 batch 4，200 步，每 10 步存一个离线评
- **每存一个就评一个、评完立刻删，绝不允许攒着**（`relearning.hard_constraint`）：200 步 / `save_steps=10` = 20 个 ckpt，攒起来是 **50 G**——叠在 14 G 固定开销和已保留的各方法 A 之上就会爆 85 G 卷；评完即删的话峰值只多 2.5 G
- 评测照常跑全部 400 条，只对 held-out 那 360 条的 `value_by_index` 取平均——不需要自建评测集
- 每个评测点各用自己的 `output_dir`（§2.1）
- **不要用 GA 做这个**：GA 不回弹很可能是模型已经坏了，不是知识被删了，这一轴上是混淆的

一张图："这个方法看起来忘干净了，40 条数据 N 步就全回来了。"全场最有冲击力的一页。

---

## 5. 什么时候停下来问我

只有四条。触发时：停、把已完成的产物列给我、把实测值和阈值列给我，**然后等指示。不许自己改库代码、seed、学习率或阈值来绕过。**

1. **Gate 1a 不过**（§3）
2. **Gate 2 前三条任一不过**（第 4 条不过是走分支，不是停机 —— 见段 C）
3. **某方法判定为不可行** → 报告后**这一条可以继续**，该方法退出跨方法比较，其余照跑
4. **要删任何训练产物，或磁盘剩余低于阈值**

另外三种情况直接告诉我但不用停：显存装不下 `per_device=8`、RMU_downproj 的 lr ladder 用完仍到不了操作点、8 次 restoration 的离散度非 0。

---

## 6. 产物

**只要求存在和可复现，格式你定。** 但这几样必须留下，因为报告要用：

- 环境快照：`pip freeze`、repo commit、torch / transformers 版本、GPU 型号、**`attn_implementation`、`torch_dtype`、评测 batch size**（后三个决定 Gate 1a 和确定性检查能不能复现，别漏）
- 每个 gate 的判定结果：实测值、阈值、过没过
- 操作点：轨迹（每个 ckpt 的步数与 `forget_Q_A_ROUGE`；utility 只有三个点，缺的地方留空不要插值）+ 选中的 A + `feasible` / `feasible_check` + overshoot
- 每层每方法：恢复量、归一化恢复量、bootstrap CI、是否显著、`‖Δθ_layer‖`
- 所有图的底层数据（不只是图片）

**磁盘**（RunPod：network volume `/workspace` 85 G 配额跨停机保留 + container disk 停止即清空）：

| | |
|---|---|
| container disk `/root` | 只放 `/root/tmp_restore`（扫层临时权重，用完即删）和日志。**停机清空，不放任何要留的东西** |
| volume `/workspace` | conda + 环境 + repo + HF 缓存 + results，固定开销约 **14 G**；其余全给 ckpt。单方法训练期间 = 12 个 ckpt + 1 份最终权重 = **32.5 G**；已保留的各方法操作点 A = 每个 **2.5 G** |
| **峰值** | 卷上约 **56.5 G / 85 G**（14 固定 + 32.5 当前方法 + 4 个方法的 A 共 10），出现在**段 A 第 5 步训第 5 个方法时**、以及**段 C 训 RMU_downproj 时**，两处相当。停机线 `disk_stop_free_gb=8`，按 `85 − du -s /workspace` 估算剩余（`run_pipeline.py:check_disk`） |

**四条硬规则，任何一条不执行都会爆盘**（85 G 没有让任何一条变松）：

| # | 规则 | 位置 | 不守的后果 |
|---|---|---|---|
| 1 | 严格串行：训一个 → 选 A → 清理 → 训下一个 | §4 段 A 第 5 步 | 5 × 32.5 = 162.5 G |
| 2 | 定完 A 立刻清理，只留 A | §4 段 A 第 6 步 | 同上 |
| 3 | 扫层临时模型目录任一时刻最多一个 | §4 段 A 第 9 步 | 16 × 2.5 = 40 G |
| 4 | relearning 的 ckpt 评完即删 | §4 段 F | 20 × 2.5 = 50 G |

泄压阀：扫层完成的方法，其 A 可删（§4 段 A 第 6 步末），进段 C 前执行一次，卷上峰值降到约 46.5 G。
